```c
void createQuotedString(undefined *outBuffer, char **inputString)
{
  size_t inputLength;
  
  inputLength = strlen(*inputString); // Get the length of the input string
  *outBuffer = 0x22; // Start the output with a double-quote character
  
  // If the input string length is greater than 78 characters, truncate it
  if (0x4e < inputLength) {
    strncpy(outBuffer + 1, *inputString, 0x4e); // Copy first 78 characters of input into output buffer
    outBuffer[0x4f] = 0x22; // Close the quoted string with a double-quote character
    return;
  }
  
  // If the input string is shorter, copy the whole string into the output buffer
  strncpy(outBuffer + 1, *inputString, inputLength);
  outBuffer[inputLength + 1] = 0x22; // Close the quoted string with a double-quote character
  
  // If the input string was not exactly 78 characters long, add a null terminator
  if (inputLength != 0x4e) {
    outBuffer[inputLength + 2] = 0;
  }
  return;
}
```