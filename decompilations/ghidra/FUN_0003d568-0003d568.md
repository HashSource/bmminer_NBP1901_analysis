```c
void GetNonNegativeParameterValue(undefined4 configParameterName, int *valueOutput)
{
  int functionResult;
  int valueArray[2];
  
  // Attempts to retrieve the parameter value, stores in valueArray
  functionResult = RetrieveParameterValue(configParameterName, valueArray);
  
  // If retrieval is successful, proceed
  if (functionResult == 0) {
    // Set the output parameter to the retrieved value
    *valueOutput = valueArray[0];
    
    // Check if the value is negative
    if (valueArray[0] < 0) {
      // If negative, log a message indicating the issue
      LogError("\'%s\' is negative", configParameterName);
      return;
    }
  }
  // Function exits, negative check is only performed on successful retrieval
  return;
}
```