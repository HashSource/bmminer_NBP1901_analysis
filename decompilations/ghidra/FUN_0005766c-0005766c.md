```c
#include <stdlib.h> // Required for free()

// Forward declaration of the logging functions
void LogDebugMessage(const char* filePath, int line, const char* format, ...);
void LogErrorMessage(const char* filePath, int line, const char* format, ...);
void SomeFunctionThatLogs(); // Placeholder for the unidentified function

// Frees the memory allocated for a logging format object and logs the action
// param_1 should point to the memory location of the format object
void FreeLoggingFormat(void *formatObject)
{
  // Check if the format object pointer is not NULL
  if (formatObject != NULL) {
    // Check if some condition at an offset 0x2004 from the format object base is nonzero
    if (*(int *)((int)formatObject + 0x2004) != 0) {
      // Call an unidentified logging related function
      SomeFunctionThatLogs();
    }
    // Log the removal of the format object using the debug priority
    LogDebugMessage("src/format.c", 0x31, "zlog_format_del[%p]", formatObject);
    // Free the memory associated with the format object
    free(formatObject);
  } else {
    // Log the error if the format object pointer is NULL
    LogErrorMessage("src/format.c", 0x2d, "a_format is null or 0");
  }
}
```