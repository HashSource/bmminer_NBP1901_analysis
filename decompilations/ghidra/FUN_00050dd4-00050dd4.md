```c
void InitializeMinerHardware(undefined4 minerId)
{
  // Initialize the miner with specified configurations
  // minerId: Identifier for the miner hardware
  // Enable first two arguments, disable the next two
  ConfigureMinerHardware(minerId, true, false, true, false);
  return;
}
```