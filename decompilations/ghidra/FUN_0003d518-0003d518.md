```c
void getConfigValueAsInteger(undefined4 configKey, int *intValue)
{
  int operationStatus;
  int valueBuffer[2];
  
  operationStatus = parseConfigValue(configKey, valueBuffer);
  if (operationStatus == 0) {
    if (valueBuffer[0] < 0) {
      logError("\'%s\' is negative", configKey);
      return;
    }
    *intValue = valueBuffer[0];
  }
  return;
}
```
