```c
#define MAX_MINER_COUNT 4

// CheckAllMinersStatus - Checks the status of all miners and returns if any are active
undefined4 CheckAllMinersStatus(void)
{
  int minerIndex;
  int minerInitializationStatus;
  int minerOperationalStatus;
  
  // Loop through each miner and check its status
  for (minerIndex = 0; minerIndex < MAX_MINER_COUNT; minerIndex++) {
    // Check if the miner is initialized successfully
    minerInitializationStatus = CheckMinerInitialized(minerIndex);
    if (minerInitializationStatus == 0) { // Miner is not initialized
      continue; // Skip to the next miner
    }
    
    // Check if the miner is operational (1 means operational)
    minerOperationalStatus = CheckMinerOperational(minerIndex);
    if (minerOperationalStatus != 1) { // Miner is not operational
      continue; // Skip to the next miner
    }
    // If this point is reached, at least one miner is operational
    return 1;
  }
  
  // If we reach here, no miners are operational
  return 0;
}
```