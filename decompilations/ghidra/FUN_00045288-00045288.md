```c
void SetMinerFrequency(uint16_t frequency)
{
  uint8_t frequencyDataBuffer[12];
  
  SendFrequencyCommand(0x28, frequency); // Send frequency setting command to miner
  ReceiveFrequencyResponse(0x28, frequencyDataBuffer); // Receive response from miner after setting frequency
  return;
}
```