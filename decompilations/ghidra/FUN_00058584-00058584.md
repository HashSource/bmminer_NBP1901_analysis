```c
void deleteFile(void *fileStructPointer)
{
  // Log the file deletion start with the memory address of the file structure
  logDebug(0, "src/rotater.c", 126, "Deleting file[%p]", fileStructPointer);
  // Log the path of the file that is being deleted
  logDebug(0, "src/rotater.c", 127, "File path[%s]", (int)fileStructPointer + 4);
  // Free the memory allocated for the file structure
  free(fileStructPointer);
}
```