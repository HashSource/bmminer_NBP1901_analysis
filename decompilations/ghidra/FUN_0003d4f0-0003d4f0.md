```c
void getTemperatureSensorValue(undefined4 sensorId, undefined4 *temperatureValuePtr) {
  int getTemperatureSuccess;
  undefined4 readTemperatureValue;

  // Attempt to read the temperature from the sensor
  getTemperatureSuccess = readTemperature(sensorId, &readTemperatureValue);
  
  // If the read was successful, update the temperature value
  if (getTemperatureSuccess == 0) {
    *temperatureValuePtr = readTemperatureValue;
  }
  // No need to handle else case, as the temperature value remains unchanged on failure.
  
  return;
}
```