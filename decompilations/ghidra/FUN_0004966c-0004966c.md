```c
#include <pthread.h>

// Global mutex variable used by FUN_0004966c()
pthread_mutex_t gI2CLockMutex = PTHREAD_MUTEX_INITIALIZER;

// Prototype of the previously decompiled function FUN_00049cfc()
// The name should be updated to its purpose when known
extern undefined4 PerformI2CAccess(undefined4 param_1, undefined4 param_2, undefined4 param_3);

// Custom error message for I2C lock failure
const char gFailedToI2CLockMessage[] = "Failed to acquire I2C lock";

// This function seems to acquire a mutex before attempting I2C access
undefined4 AcquireLockAndPerformI2CAccess(undefined4 param_1, undefined4 param_2, undefined4 param_3) {
  int lockResult;
  undefined4 functionResult;
  // Local buffer for error message, its size suggests it's related to failed mutex lock operation
  char errorMessageBuffer[20];
  
  // Try to lock the mutex before accessing I2C
  lockResult = pthread_mutex_lock(&gI2CLockMutex);
  if (lockResult == 0) {
    // If mutex is successfully locked, proceed with I2C access
    functionResult = PerformI2CAccess(param_1, param_2, param_3);
    // Unlock mutex after I2C access operation
    pthread_mutex_unlock(&gI2CLockMutex);
  }
  else {
    // If mutex lock failed, set function result to error code (e.g., -4)
    functionResult = 0xfffffffc;
    // Copy the error message "Failed to acquire I2C lock" to the buffer
    memcpy(errorMessageBuffer, gFailedToI2CLockMessage, sizeof(gFailedToI2CLockMessage));
    // Presumably, log the error message (function name should be updated when known)
    LogErrorMessage(0, errorMessageBuffer);
  }
  return functionResult;
}

// Prototype of the logging function, which should be renamed to its actual name when known
extern void LogErrorMessage(int logLevel, char *message);
```