```c
float calculateHashrate(int chipIndex, int coreIndex) {
  uint hashCount;
  int clockSpeed;
  int voltageLevel;
  
  // Retrieve the number of hashes calculated by a specific core on a specific chip
  hashCount = *(uint *)((coreIndex + (chipIndex + hashCountRegisterOffset)) * 4);
  // Get the clock speed setting for the chip using a previously-defined function
  clockSpeed = getClockSpeed();
  // Get the voltage level setting for the chip using a previously-defined function
  voltageLevel = getVoltageLevel();
  // Compute and return the hashrate by normalizing the hash count with the clock speed,
  // voltage level, and a constant factor (8 in this case)
  return (float)hashCount / (float)(voltageLevel * clockSpeed * constantFactor);
}
```