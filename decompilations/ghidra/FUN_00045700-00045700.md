```c
#include <pthread.h>

// Declaring a global mutex variable; assuming it's declared elsewhere in the code
extern pthread_mutex_t globalMutex;

void updateSystemStatus(void)
{
  // Local variable to store status data; the array size is inferred from the stack allocation
  unsigned char statusData[4];
  
  // Lock the global mutex before updating the system status
  pthread_mutex_lock(&globalMutex);

  // Assuming FUN_00040314 updates system status.
  // Calling the function to update the status and passing the statusData array to it.
  recordSystemStatus(0x1b, statusData); // Renamed FUN_00040314 to recordSystemStatus for clarity
  
  // Unlock the global mutex after updating is done.
  pthread_mutex_unlock(&globalMutex);
  
  // End of function
}
```