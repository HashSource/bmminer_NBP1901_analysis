```c
void InitializeControlStructure(uint32_t *controlStructPtr)
{
  uint32_t arraySizeBitIndex;
  uint32_t *currentArrayPtr;
  uint32_t *currentArrayEndPtr;
  uint32_t *dataPtr;
  uint32_t *nextDataPtr;
  
  ResetControlStructure(); // Renamed from FUN_0004b8ac to ResetControlStructure
  dataPtr = controlStructPtr + 3; // Data pointer set to fourth element of control structure
  arraySizeBitIndex = controlStructPtr[2]; // Bit index for array size calculation
  if (1 << (arraySizeBitIndex & 0xff) != 0) { // Check if array size is not zero
    currentArrayPtr = (uint32_t *)controlStructPtr[1]; // Current array pointer set to second element of control structure
    currentArrayEndPtr = currentArrayPtr; // End pointer initially set to start of array
    do {
      currentArrayEndPtr[1] = dataPtr; // Set the second element of current pair to the data pointer
      nextDataPtr = currentArrayEndPtr + 2; // Move to the next pair
      *currentArrayEndPtr = dataPtr; // Set the first element of current pair to the data pointer
      currentArrayEndPtr = nextDataPtr; // Update the end pointer to the next pair
      // Loop until we reach the end of the array calculated by array size bit index
    } while (nextDataPtr != (uint32_t *)((int)currentArrayPtr + (8 << (arraySizeBitIndex & 0xff))));
  }
  // Initialize additional pointers and counter in the control structure
  controlStructPtr[4] = dataPtr; // Fifth element points to data
  controlStructPtr[3] = dataPtr; // Fourth element points to data (same as 5th)
  controlStructPtr[6] = controlStructPtr + 5; // Seventh element points to the sixth element's address
  controlStructPtr[5] = controlStructPtr + 5; // Sixth element points to itself
  *controlStructPtr = 0; // First element (counter) set to zero
  return;
}
```