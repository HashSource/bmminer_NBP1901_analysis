```c
void ResetHardwareErrorCounters(void)
{
  // Lock the mutex to ensure thread safety when manipulating shared resources
  pthread_mutex_lock((pthread_mutex_t *)&HardwareErrorMutex);

  // Call the function to clear the hardware error log or counter
  ClearHardwareErrorLog(HardwareErrorLog);

  // Reset hardware error counters to zero
  HardwareErrorCount = 0;
  ConsecutiveHardwareErrorCount = 0;
  
  // Unlock the mutex after finishing the operations
  pthread_mutex_unlock((pthread_mutex_t *)&HardwareErrorMutex);
  return;
}
```