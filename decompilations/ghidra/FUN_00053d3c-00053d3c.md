```c
int RemoveLoggingKey(int logKey)
{
  int lockResult;
  void *threadSpecificData;
  int removalResult;
  
  // If the logKey is null, log an error message and return 0.
  if (logKey == 0) {
    LogError(2,"src/zlog.c",0x1f8,"key is null or 0");
    removalResult = 0;
  }
  else {
    // Attempt to acquire a read lock on the logging data structure.
    lockResult = pthread_rwlock_rdlock((pthread_rwlock_t *)&GlobalLoggingLock);
    if (lockResult == 0) {
      // Check if the logging system has been initialized.
      if (GlobalLoggingInitialized == 0) {
        LogError(2,"src/zlog.c",0x201,"never call zlog_init() or dzlog_init() before");
      }
      else {
        threadSpecificData = pthread_getspecific(ThreadSpecificDataKey);
        // If no thread specific data is found, log an error message.
        if (threadSpecificData == NULL) {
          LogError(2,"src/zlog.c",0x207,"thread not found, maybe not use zlog_put_mdc before");
        }
        else {
          // Attempt to remove the logging key from the thread's specific data.
          removalResult = RemoveKeyFromThreadLog(*(undefined4 *)((int)threadSpecificData + 4),logKey);
          // If key removal is not successful, unlock read lock and return the result.
          if (removalResult != 0) {
            lockResult = pthread_rwlock_unlock((pthread_rwlock_t *)&GlobalLoggingLock);
            if (lockResult == 0) {
              return removalResult;
            }
            LogError(2,"src/zlog.c",0x213,"pthread_rwlock_unlock fail, rc=[%d]",lockResult);
            return 0;
          }
          // If the key is not found, log the message.
          LogError(2,"src/zlog.c",0x20d,"key[%s] not found in mdc",logKey);
        }
      }
      // Unlock the read lock after use.
      removalResult = pthread_rwlock_unlock(&ThreadLock);
      if (removalResult != 0) {
        LogError(2,"src/zlog.c",0x21a,"pthread_rwlock_unlock fail, rc=[%d]",removalResult);
        return 0;
      }
    }
    else {
      removalResult = 0;
      // If acquiring read lock fails, log an error message.
      LogError(2,"src/zlog.c",0x1fc,"pthread_rwlock_rdlock fail, rc[%d]",lockResult);
    }
  }
  return removalResult;
}
```

Each function, variable and error message has been renamed to make the code more self-explanatory. Comments have been added for clarity on critical parts of the code logic.