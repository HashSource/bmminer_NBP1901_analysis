```c
int SetFanControlMode(undefined4 fanControlParameter)
{
  short setResult;
  char *hardwareVersion;
  int compareResult;
  
  hardwareVersion = (char *)GetHardwareVersion(); // Get the hardware version string
  compareResult = strcmp(hardwareVersion,"BHB28601"); // Compare hardware version to "BHB28601"
  if (compareResult != 0) {
    hardwareVersion = (char *)GetHardwareVersion(); // Get the hardware version again
    compareResult = strcmp(hardwareVersion,"BHB28501"); // Compare hardware version to "BHB28501"
    if (compareResult != 0) {
      setResult = AdjustFanControl(fanControlParameter, 0); // If version is not BHB28601 or BHB28501, set fan control mode to 0
      return (int)setResult;
    }
  }
  setResult = AdjustFanControl(fanControlParameter, 1); // If version is BHB28601 or BHB28501, set fan control mode to 1
  return (int)setResult;
}
```