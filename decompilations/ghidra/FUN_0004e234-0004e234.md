```c
undefined4 CheckAndResetErrorCounter(int *errorCounterPtr, int resetFlag)
{
  undefined4 resetStatus;
  
  // Check if the pointer is not NULL, resetFlag is true, and error counter is zero
  if ((errorCounterPtr != (int *)0x0 && resetFlag != 0) && (*errorCounterPtr == 0)) {
    // Call the reset function and return its status
    resetStatus = ResetErrorCounter(errorCounterPtr + 2);
    return resetStatus;
  }
  // If conditions are not met, return 0
  return 0;
}
```