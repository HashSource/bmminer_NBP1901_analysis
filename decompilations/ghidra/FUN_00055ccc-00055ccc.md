```c
void DisplayCategoryTable(int categoryTablePtr, undefined4 logLevel)
{
  int categoryItemPtr;
  
  // If the categoryTablePtr is NULL, log an error and exit 
  if (categoryTablePtr == 0) {
    LogError(2, "src/category_table.c", 21, "categories is null or 0");
    return;
  }
  
  // Log the address of the category table
  LogInfo(logLevel, "src/category_table.c", 22, "-category_table[%p]-", categoryTablePtr);
  
  // Iterate through the category table and display each category
  for (categoryItemPtr = GetFirstCategoryItem(categoryTablePtr);
       categoryItemPtr != 0;
       categoryItemPtr = GetNextCategoryItem(categoryTablePtr, categoryItemPtr)) {
         
    // Display the individual category pointed by categoryItemPtr
    DisplayCategoryDetails(*(undefined4 *)(categoryItemPtr + 8), logLevel);
  }
  
  return;
}
```