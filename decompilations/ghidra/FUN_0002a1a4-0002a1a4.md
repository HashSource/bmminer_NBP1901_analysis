```c
void FreeMinerResources(void *minerData)
{
  // Free memory allocated for specific miner resources based on offsets in the data structure.
  free(*(void **)((int)minerData + 0x120)); // Free resource at offset 0x120
  free(*(void **)((int)minerData + 0x134)); // Free resource at offset 0x134
  free(*(void **)((int)minerData + 0x148)); // Free resource at offset 0x148
  free(*(void **)((int)minerData + 0x140)); // Free resource at offset 0x140

  // Clear the memory block by setting all bytes to zero, effectively resetting the data structure.
  memset(minerData,0,0x1c0);

  return;
}
```