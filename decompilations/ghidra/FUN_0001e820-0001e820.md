```c
// Function to update the configuration of a mining device.
// Param 'deviceIndex' identifies the target device.
// Param 'configOffset' is the offset to the specific configuration setting.
// Param 'configValue' is the value to be set in the configuration.
int UpdateMiningDeviceConfig(uint deviceIndex, int configOffset, undefined configValue)
{
  int updateStatus;
  uint isOutOfRange;
  
  // Check if the device index is out of range by shifting it 31 bits to the right
  // and comparing it with a predefined limit.
  isOutOfRange = deviceIndex >> 0x1f;
  if (MAX_DEVICES_LIMIT <= (int)deviceIndex) {
    isOutOfRange = 1;
  }

  // If the device index is within valid range
  if (isOutOfRange == 0) {
    // Check if a global condition or flag is met (this variable might represent a state or mode)
    if (SomeGlobalCondition == 0) {
      // If the global condition is not set, call a function to perform a certain action
      // This is likely to be some initialization or validation routine
      updateStatus = InitOrValidateDevice(deviceIndex);
      if (updateStatus < 0) {
        // If initialization or validation fails, return the error code
        return updateStatus;
      }
    }
    else {
      updateStatus = 0; // Assume everything is alright if the global condition is set
    }

    // Update the configuration value for the device; 'CONFIG_ARRAY_BASE' could be
    // a pointer to the start of a configuration array with 'CONFIG_SIZE_PER_DEVICE' as the size of each device's config
    *(undefined *)(CONFIG_ARRAY_BASE + deviceIndex * CONFIG_SIZE_PER_DEVICE + configOffset + CONFIG_OFFSET) = configValue;
    // Return the update status which would be 0 if successful
    return updateStatus;
  }

  // If the device index is out of range, return an error code
  return OUT_OF_RANGE_ERROR;
}
```