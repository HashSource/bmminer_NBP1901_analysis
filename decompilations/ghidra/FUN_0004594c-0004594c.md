```c
void SetFanSpeed(uint fanID)
{
  DisableInterrupts(); // Disable interrupts with priority level 13
  EnableFanPWMControl(1 << (fanID & 0xff)); // Enable PWM control on the specified fan
  return;
}
```