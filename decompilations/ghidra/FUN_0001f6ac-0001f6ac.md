```c
/* WARNING: Globals starting with '_' overlap smaller symbols at the same address */

void UpdateMiningStatistics(int jsonHandle)
{
  // Define variable types for local variables
  float averageMiningRate;
  float localMiningData1;
  float localMiningData2;
  float localMiningData3;
  char formattedMessage[2048]; // Buffer to store formatted strings
  
  // Initialize local variables storing mining data
  averageMiningRate = 0.0f;
  localMiningData1 = 0.0f;
  localMiningData2 = 0.0f;
  localMiningData3 = 0.0f;
  
  // Check if the input parameter is valid
  if (jsonHandle == 0) {
    // Check global conditions to see if a warning should be logged
    if ((GlobalFlag1 != '\0' || GlobalFlag2 != '\0') || (GlobalCounter > 2)) {
      // Prepare a warning message
      snprintf(formattedMessage, 0x800, "%s: input bad json param\n", GlobalErrorMsg);
      // Log the warning message
      LogWarning(3, formattedMessage, 0);
    }
    // Early return since the provided jsonHandle was not valid
    return;
  }
  
  // Calculate and format the average mining rate
  snprintf((char *)&averageMiningRate, 0x10, "%.2lf", GlobalMiningData,
           GlobalData1 / ((double)(longlong)GlobalData2 - (double)(longlong)GlobalMiningData));
  // Convert and store the formatted average mining rate into a variable
  float formattedAverageMiningRate = ConvertStringToFloat(&averageMiningRate);
  // Update the JSON handle with the key "rate_avg" and the calculated mining rate
  UpdateJsonWithFloat(jsonHandle, "rate_avg", formattedAverageMiningRate);
  
  // Function exits after updating the statistics
  return;
}
```