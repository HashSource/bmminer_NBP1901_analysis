```c
/* WARNING: Globals starting with '_minerStatistics' overlap smaller symbols at the same address */

void ResetMinerStatistics(void)
{
  /* Local variables to store miner statistical data */
  uint32_t minerStat1;
  uint32_t minerStat2;
  uint32_t minerStat3;
  uint32_t minerStat4;
  uint32_t minerStat5;
  uint16_t minerStat6;
  
  // Check if the debug level is higher than 4.
  // If so, retrieve stats and reset them.
  if (debugLevel > 4) {
    minerStat1 = _minerStatistics.firstStat;
    minerStat2 = _minerStatistics.secondStat;
    minerStat3 = _minerStatistics.thirdStat;
    minerStat4 = _minerStatistics.fourthStat;
    minerStat5 = _minerStatistics.fifthStat;
    minerStat6 = _minerStatistics.sixthStat;
    LogResetStats(4, &minerStat1, 0);
  }
  
  // If the condition is not met and debug level is below 3, call another function to handle it.
  if ((statusIndicator == NULL) || (*(int *)(statusIndicator + 4) == NULL)) {
    if (debugLevel < 3) {
      HandleMinerOperation();
      return;
    }
  }
  else {
    // If the condition is met, execute the function pointed by the function pointer in the structure pointed by statusIndicator.
    // The function pointer is at offset 0xc of the structure.
    (*(void (*)(void))(*(int *)(statusIndicator + 4) + 0xc))();
  }
  // The function does not return any value.
}
```