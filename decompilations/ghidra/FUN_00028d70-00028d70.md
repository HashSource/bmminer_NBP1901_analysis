```c
char * checkValueOutOfRange(undefined4 errorCode, uint *value)
{
  char *errorString;
  
  // Call another function, possibly to convert error code to error string
  errorString = (char *)convertErrorCodeToString();
  
  // Return the error string if it is not NULL
  if (errorString != NULL) {
    return errorString;
  }
  
  // Set default error string to NULL
  errorString = NULL;
  
  // If value is greater than 100, it's out of range
  if (100 < *value) {
    errorString = "Value out of range";
  }
  
  // Return the error string, either NULL or "Value out of range"
  return errorString;
}
```