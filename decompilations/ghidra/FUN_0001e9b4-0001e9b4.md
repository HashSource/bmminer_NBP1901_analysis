```c
void SetMinerConfigFlags(undefined4 minerId, undefined4 configOption) {
  uint configFlags;
  
  // Function to read the current configuration flags from miner
  // Parameters: (operationMode, minerId, configOption, size, pointerToConfigFlags)
  ReadMinerConfigFlags(READ_OP, minerId, configOption, 0x18, &configFlags);
  
  // Modify the configuration flag - ensure the 4th bit is set to 1 (bitmask: 0x8)
  configFlags = (configFlags & ~0x8) | 0x8;
  
  // Function to write the modified configuration flags back to miner
  // Parameters: (minerId, operationMode, configOption, size, newConfigFlags)
  WriteMinerConfigFlags(minerId, WRITE_OP, configOption, 0x18, configFlags);
  
  return;
}
```