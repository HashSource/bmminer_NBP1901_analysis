```c
// Function that initializes message handling object
void InitMessageHandler(undefined4 messageID)
{
  // Declare a pointer to hold the address of the allocated memory for the message handler
  undefined4 *messageHandlerPtr;

  // Allocate memory for the message handler (24 bytes or 6 elements of undefined4)
  messageHandlerPtr = (undefined4 *)malloc(0x18);
  
  // Assign the message ID to the fourth element in the message handler array
  messageHandlerPtr[3] = messageID;
  
  // Set up pointers to functions:
  // 1. Assign the address of the HandleMessage function to the first element in the array
  *messageHandlerPtr = &HandleMessageEntryPoint;
  
  // 2. Assign the address of the InitializeMessage function to the third element in the array
  messageHandlerPtr[2] = &InitializeMessageEntryPoint;
  
  // 3. Assign the address of the CleanupMessage function to the second element in the array
  messageHandlerPtr[1] = CleanupMessage;
  
  // Initialize the fifth and sixth elements in the message handler array to 0 (null pointers)
  messageHandlerPtr[4] = 0;
  messageHandlerPtr[5] = 0;
  
  // No return value is needed, initialization is performed on the allocated object
}
```