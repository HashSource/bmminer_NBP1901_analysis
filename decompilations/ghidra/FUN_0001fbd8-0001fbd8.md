```c
void UpdateHashrateStatistic(int fileDescriptor, uint chipIndex)
{
  // Variable initialization
  undefined4 hashrateValue;
  undefined4 placeholderVariable1;
  undefined4 placeholderVariable2;
  undefined4 placeholderVariable3;
  undefined4 placeholderVariable4;
  char errorMessageBuffer[2048];
  
  // Placeholder variables are set to 0 initially
  placeholderVariable1 = 0;
  placeholderVariable2 = 0;
  placeholderVariable3 = 0;
  placeholderVariable4 = 0;

  // Check if the fileDescriptor is valid and chipIndex is less than 4
  if (fileDescriptor != 0 && chipIndex < 4) {
    // Retrieve the ideal hashrate for the given chip index
    hashrateValue = RetrieveIdealHashrate(chipIndex);
    snprintf((char *)&placeholderVariable1, 0x10, "%d", hashrateValue);
    hashrateValue = ProcessHashrateValue(&placeholderVariable1);
    writeHashrateToFile(fileDescriptor, "rate_ideal", hashrateValue);
    return;
  }

  // Conditions to determine if error logging is to be bypassed
  if ((UnkGlobalCondition == '\0') && (SomeOtherGlobalFlag == '\0') && (ErrorCountGlobal < 3)) {
    return;
  }

  // Log error message if parameters are bad
  snprintf(errorMessageBuffer, 0x800, "%s: input bad param\n", ErrMsgPrefix);
  LogErrorMessage(3, errorMessageBuffer, 0);
  return;
}
```

Comments explaining each part of the code have been added, and the function names and variables have been renamed to provide a clearer understanding of their intended purpose.