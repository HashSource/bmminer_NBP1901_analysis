```c
/* Function Name: SetMinerConfiguration */
undefined4 SetMinerConfiguration(undefined4 minerID, int fanControlEnabled, int asicBoostEnabled, undefined2 frequencyCode)
{
  ushort updated_data_upper_half;
  undefined minerConfigByte;
  undefined4 operation_status;
  byte fanControlBit;
  undefined4 current_config;

  /* Retrieve the current configuration for the miner. */
  ReadMinerConfig(0, minerID, 0, 0xa4, &current_config);

  /* Configure Fan Control: Set or clear the fan control bit based on the provided flag. */
  if (fanControlEnabled == 0) {
    fanControlBit = current_config._3_1_ & 0x7f;
  }
  else {
    fanControlBit = current_config._3_1_ | 0x80;
  }
  /* Update fan control bit in the configuration. */
  current_config = CONCAT13(fanControlBit, (undefined3)current_config);

  /* Get the upper half of the configuration data which may include frequency control bits. */
  updated_data_upper_half = (ushort)(current_config >> 0x10);

  /* Configure AsicBoost: Update configuration data based on the provided AsicBoost mode. */
  if (asicBoostEnabled == 4) { // 4 implies low power mode.
    current_config._2_2_ = updated_data_upper_half & 0xcfff;
  }
  else if (asicBoostEnabled == 8) { // 8 implies standard mode.
    current_config._2_2_ = updated_data_upper_half & 0xcfff | 0x1000;
  }
  else {
    if (asicBoostEnabled != 0xc) { // 0xc implies enhanced mode.
      return 0xffffffff; // Return an error if an unsupported mode is specified.
    }
    /* Set enhanced mode bits in the configuration. */
    current_config._2_2_ = (ushort)(CONCAT13(fanControlBit, (undefined3)current_config) >> 0x10) & 0xcfff | 0x2000;
  }

  /* Update the configuration with the updated configuration data. */
  minerConfigByte = current_config._3_1_;
  /* Set frequency code and keep the upper bits of the configuration data. */
  current_config = CONCAT22(current_config._2_2_, frequencyCode);
  /* Restore the miner configuration byte and clear the reserved bit (bit30) to ensure it is 0. */
  current_config = CONCAT13(minerConfigByte, (undefined3)current_config) & 0xbfffffff;

  /* Write the updated configuration back to the miner. */
  operation_status = WriteMinerConfig(minerID, 1, 0, 0xa4, current_config);

  return operation_status; // Return operation status, 0 indicates success, non-zero indicates error.
}
```