```c
#define BUFFER_OFFSET 0x1f

typedef unsigned int uint32_t;

// Copy the content of one buffer to another
// param_1: index of the source buffer
// param_2: destination buffer pointer
// param_3: number of bytes to copy
uint32_t CopyBufferContents(int srcBufferIndex, int dstBufferPtr, int bytesToCopy)
{
  unsigned char *srcBytePtr;  // Pointer to the current byte in the source buffer
  unsigned char *dstBytePtr;  // Pointer to the current byte in the destination buffer
  
  // Check if we have any bytes to copy
  if (bytesToCopy != 0) {
    // Start at the end of the fixed portion of the source buffer and offset by the buffer index
    srcBytePtr = (unsigned char *)(DAT_00505108 + srcBufferIndex * 0x40 + BUFFER_OFFSET);
    
    // Point to the byte before the beginning of the destination, so when we first add 1 we get the correct starting position
    dstBytePtr = (unsigned char *)(dstBufferPtr - 1);
    
    // Copy byte by byte from source to destination until we reach the required number of bytes
    do {
      srcBytePtr++;  // Increment the source byte pointer
      dstBytePtr++;  // Increment the destination byte pointer
      *dstBytePtr = *srcBytePtr;  // Copy the byte from source to destination
    } while (dstBytePtr != (unsigned char *)(dstBufferPtr + bytesToCopy - 1));
  }
  
  return 0;  // Return successful completion
}
```