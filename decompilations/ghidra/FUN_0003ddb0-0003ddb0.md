```c
void FreeMiningBuffer(void)
{
  // Free the allocated memory for the mining buffer
  free(miningBufferPtr);
  // Set the pointer to NULL after freeing to avoid dangling pointer
  miningBufferPtr = NULL;
  return;
}
```