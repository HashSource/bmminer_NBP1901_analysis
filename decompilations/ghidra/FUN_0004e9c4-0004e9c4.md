```c
#include <stdint.h>

// Function: GetMiningStatus
// Description: Retrieves the mining status from a given status structure.
// Arguments:
//   - statusPtr: A pointer to a status structure containing mining information.
// Return:
//   - If statusPtr is valid and the mining status is running ('3'), return the associated status value.
//   - Otherwise, return zero (indicating an uninitialized or stopped state or invalid pointer).
uint64_t GetMiningStatus(int *statusPtr)
{
  // Check if the pointer is non-null and the mining status is running (indicated by value '3')
  if ((statusPtr != NULL) && (*statusPtr == 3)) {
    // Return the status value (assuming it is stored at an offset of 2 integers from the start of the structure)
    return *(uint64_t *)(statusPtr + 2);
  }
  // Return zero in case of null pointer or status not equal to '3'
  return 0;
}
```