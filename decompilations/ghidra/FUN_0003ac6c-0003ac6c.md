```c
#include <stdint.h>

#define SUCCESS_CODE 0

uint32_t GetStatusDifference(void)
{
  int hardwareStatus;
  int softwareStatus;
  uint32_t updateStatus;
  
  hardwareStatus = GetHardwareStatus(); // renamed thunk_FUN_00019ab4 to GetHardwareStatus
  softwareStatus = GetSoftwareStatus(); // renamed FUN_000361f4 to GetSoftwareStatus
  
  // Compare hardware and software statuses
  if (hardwareStatus != softwareStatus) {
    // If they differ, perform a status update
    updateStatus = UpdateSystemStatus(); // renamed FUN_00036208 to UpdateSystemStatus
    return updateStatus; // Return the result of the update
  }
  
  // If statuses are the same, no need to update, return success code
  return SUCCESS_CODE;
}
```
