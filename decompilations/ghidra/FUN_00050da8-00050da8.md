```c
void configureASICRegisters(undefined4 chip_id, undefined4 register_address) {
  // Configure specific ASIC registers by writing a value
  writeASICRegister(chip_id, 0, register_address, 0x3c, 0x8000851f);
  // Sleep for 10,000 microseconds (or 10 milliseconds)
  usleep(10000);
  // Write another value to the same register after the delay
  writeASICRegister(chip_id, 0, register_address, 0x3c, 0x80000600);
  // End of configuration function
}
```