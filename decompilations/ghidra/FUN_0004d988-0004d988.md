```c
void resetHardwareMonitorData(int *hardwareMonitorDataPtr)
{
  // If the hardware monitor data is not already zero, call another function to handle it
  if (*hardwareMonitorDataPtr != 0) {
    handleNonZeroMonitorData(); // Original function name: FUN_0004d898
  }
  // Reset the values in the monitor data structure to zero
  hardwareMonitorDataPtr[2] = 0; // Possibly an error count or other status indicator
  hardwareMonitorDataPtr[1] = 0; // Possibly an intermediate value or state indicator
  *hardwareMonitorDataPtr = 0;   // Possibly the main status flag indicating operation status
  return;
}
```
