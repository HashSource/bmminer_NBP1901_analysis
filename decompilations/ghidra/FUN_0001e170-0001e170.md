```c
// Function that retrieves the current system status.
//
// This function returns an unsigned 32-bit integer representing the system status,
// which is stored in a global variable.

unsigned int GetSystemStatus(void)
{
  // Global variable holding the system status; assumed to be initialized elsewhere.
  extern unsigned int gSystemStatus;

  // Return the current value of the system status.
  return gSystemStatus;
}
```