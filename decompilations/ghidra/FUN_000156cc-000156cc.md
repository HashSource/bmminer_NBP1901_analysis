```c
int CheckMinerModelAndSetFrequencyOffset(undefined4 frequencyParam)
{
  short frequencyOffsetResult;
  char *minerModel;
  int modelComparisonResult;
  
  minerModel = (char *)GetMinerModelString();
  modelComparisonResult = strcmp(minerModel,"BHB28601");
  if (modelComparisonResult != 0) {
    // If the model is not "BHB28601", check for "BHB28501"
    minerModel = (char *)GetMinerModelString();
    modelComparisonResult = strcmp(minerModel,"BHB28501");
    if (modelComparisonResult != 0) {
      // If the model is neither "BHB28601" nor "BHB28501", call SetFrequencyOffset with offset 0
      frequencyOffsetResult = SetFrequencyOffset(frequencyParam, 0);
      return (int)frequencyOffsetResult;
    }
  }
  // If the model matches "BHB28601" or "BHB28501", call SetFrequencyOffset with offset 1
  frequencyOffsetResult = SetFrequencyOffset(frequencyParam, 1);
  return (int)frequencyOffsetResult;
}
```