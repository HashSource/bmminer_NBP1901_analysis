```c
// Function to perform an action based on the input parameter.
void PerformConditionalAction(int inputParameter)
{
  // Only proceed if inputParameter is non-zero.
  if (inputParameter == 0) {
    return; // Exit if inputParameter is zero.
  }
  
  // Call another function when inputParameter is non-zero.
  SomeRelatedFunction();
  
  return; // Function completed.
}
```