```c
int GetTemperatureReading(uint sensorIndex)
{
  int temperatureValue;
  
  // If sensorIndex is out of bounds (greater than the max index or negative), return error
  if (((uint)(maxSensorIndex <= (int)sensorIndex) | sensorIndex >> 0x1f) != 0) {
    return -0x7ffffbff; // Some specific error code
  }
  
  // If the sensor read function isn't initialized, initialize it before proceeding
  if ((sensorInitializedFlag == 0) && (temperatureValue = InitializeTemperatureSensor(), temperatureValue < 0)) {
    return temperatureValue; // Return error from the initialization function if failed
  }
  
  // Return the temperature value from the sensor data array, corresponding to the sensorIndex
  return *(int *)(sensorDataArray + sensorIndex * 8 + 4);
}
```