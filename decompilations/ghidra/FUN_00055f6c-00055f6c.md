```c
int AddNewCategory(int categoryTable, undefined4 categoryName, undefined4 categoryValue)
{
  int isNewCategoryCreated;
  int result;
  int addStatus;
  
  if (categoryTable == 0) {
    LogError(2, "src/category_table.c", 0x69, "categories is null or 0");
    result = 0;
  }
  else {
    isNewCategoryCreated = CheckIfCategoryExists();
    result = isNewCategoryCreated;
    if (isNewCategoryCreated == 0) {
      result = CreateNewCategory(categoryName, categoryValue);
      if (result == 0) {
        LogError(2, "src/category_table.c", 0x72, "Category creation failed");
        result = isNewCategoryCreated;
      }
      else {
        addStatus = AddCategoryToTable(categoryTable, result, result);
        if (addStatus != 0) {
          LogError(2, "src/category_table.c", 0x77, "Failed to add category to table");
          DeleteCategory(result);
          result = isNewCategoryCreated;
        }
      }
    }
  }
  return result;
}
```