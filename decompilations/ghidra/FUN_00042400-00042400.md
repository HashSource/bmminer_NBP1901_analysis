```c
#include <pthread.h>

// Global variables (likely related to thread synchronization or shared data)
pthread_mutex_t globalMutex; // Renamed from DAT_00504fec
int globalFlag1 = 0;         // Renamed from DAT_00505008
int globalFlag2 = 0;         // Renamed from DAT_0050500c
int globalFlag3 = 0;         // Renamed from DAT_00505010
int globalFlag4 = 0;         // Renamed from DAT_00505014
int globalFlag5 = 0;         // Renamed from DAT_00505018
int threadCreateStatus;      // Renamed from DAT_00505004
pthread_t monitoringThread;  // Renamed from DAT_0050501c
int initStatus = 0;          // Renamed from DAT_00504fe8

// Forward declarations for functions with unknown behavior
void InitializeMonitoring();      // Placeholder for FUN_000432d0
void *MonitoringThreadRoutine(void *arg); // Placeholder for FUN_000421c4

// Function that initializes the monitor system
void InitializeMonitorSystem(void)
{
  pthread_attr_t threadAttributes;
  
  // Initialize the mutex for thread synchronization
  pthread_mutex_init(&globalMutex, NULL);

  // Set all the flags to zero
  globalFlag1 = 0;
  globalFlag2 = 0;
  globalFlag3 = 0;
  globalFlag4 = 0;
  globalFlag5 = 0;

  // Call an unknown initialization function (possibly related to monitoring)
  threadCreateStatus = InitializeMonitoring(&MonitoringThreadRoutine, 0, 0);

  // Set up the thread attributes with a specific stack size
  pthread_attr_init(&threadAttributes);
  pthread_attr_setstacksize(&threadAttributes, 0x200000); // Stack size = 2 MB

  // Create the monitoring thread
  pthread_create(&monitoringThread, &threadAttributes, MonitoringThreadRoutine, NULL);

  // Set initialization status flag
  initStatus = 1;

  // End of the initialization function
  return;
}
```