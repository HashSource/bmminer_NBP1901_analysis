```c
void ProcessMiningData(byte *miningDataBuffer) {

  uint nonceCounter;
  byte jobID;
  byte commandType;
  undefined4 crcResult;
  int minerIndex;
  uint tempValue1;
  uint tempValue2;
  int *nonceAccumulatorPtr;
  int nonceAccumulateOffset;
  int nonceMatchOffset;
  uint jobDataWord;
  int accumulatorBase;
  uint nonceReturnOffset;
  uint nonceReturnBase;
  bool isDifferenceNegative;
  bool isArithmeticComparison;
  bool areValuesEqual;
  undefined8 multiplicationResult;
  char logBuffer[2064];

  jobID = miningDataBuffer[2];
  nonceCounter = *miningDataBuffer & 0xf;
  crcResult = CheckCRC(miningDataBuffer); // FUN_0001e160 renamed to CheckCRC
  if (nonceCounter < 4) {
    commandType = miningDataBuffer[1];
    minerIndex = GetMinerIndex(nonceCounter); // FUN_0001e29c renamed to GetMinerIndex
    if (minerIndex != 0) {
      tempValue1 = CalculateTempValue1(jobID, crcResult); // FUN_0005faf8 renamed to CalculateTempValue1
      tempValue2 = GetTempValue2(); // FUN_0001e0f0 renamed to GetTempValue2
      if (tempValue1 < tempValue2) {
        if (commandType == 0x90) {
          if ((*(uint *)(miningDataBuffer + 4) & 0x88) == 0) {
            return;
          }
          *(undefined *)(*(int *)(GLOBAL_DATA_POINTER + 4) + nonceCounter * 0x100 + tempValue1) = 1;
          return;
        }
        if (commandType == 0xe0) {
          nonceAccumulateOffset = GLOBAL_DATA_POINTER + 8;
          jobDataWord = *(uint *)(miningDataBuffer + 4) >> 0x10;
          nonceMatchOffset = nonceAccumulateOffset + (nonceCounter * 0x100 + tempValue1) * 4;
          nonceAccumulatorPtr = (int *)(NONCE_BASE_ADDRESS + nonceMatchOffset);
          nonceReturnOffset = *(uint *)(miningDataBuffer + 4) & 0xffff;
          nonceReturnBase = *(int *)(GLOBAL_DATA_POINTER + 4);
          *nonceAccumulatorPtr = *nonceAccumulatorPtr + jobDataWord;
          multiplicationResult = MultiplyByEight(nonceCounter); // FUN_0001e130 renamed to MultiplyByEight
          minerIndex = (int)((ulonglong)multiplicationResult >> 0x20);
          tempValue2 = (int)multiplicationResult * 8;
          isDifferenceNegative = SUBTRACT_OVERFLOW_FLAG(jobDataWord, tempValue2);
          isArithmeticComparison = (int)(jobDataWord - tempValue2) < 0;
          areValuesEqual = jobDataWord == tempValue2;
          if (areValuesEqual || isArithmeticComparison != isDifferenceNegative) {
            nonceAccumulatorPtr = (int *)(nonceAccumulateOffset + nonceCounter * 4);
          }
          nonceAccumulateOffset += nonceCounter * 4;
          if (areValuesEqual || isArithmeticComparison != isDifferenceNegative) {
            nonceAccumulatorPtr = nonceAccumulatorPtr + HUGE_OFFSET;
          }
          if (areValuesEqual || isArithmeticComparison != isDifferenceNegative) {
            nonceAccumulatorPtr = nonceAccumulatorPtr + LARGE_OFFSET;
          }
          if (areValuesEqual || isArithmeticComparison != isDifferenceNegative) {
            minerIndex = *nonceAccumulatorPtr;
          }
          if (areValuesEqual || isArithmeticComparison != isDifferenceNegative) {
            minerIndex += jobDataWord;
          }
          if (areValuesEqual || isArithmeticComparison != isDifferenceNegative) {
            *nonceAccumulatorPtr = minerIndex;
          }
          *(uint *)(RETURN_OFFSET_ARRAY + nonceAccumulateOffset) = *(int *)(RETURN_OFFSET_ARRAY + nonceAccumulateOffset) + (nonceReturnOffset - jobDataWord);
          *(undefined *)(nonceReturnBase + nonceCounter * 0x100 + tempValue1) = 1;
          minerIndex = MultiplyByEight(nonceCounter);
          if ((jobDataWord != minerIndex * 8 && (int)(jobDataWord - minerIndex * 8) < 0 == SUBTRACT_OVERFLOW_FLAG(jobDataWord, minerIndex * 8)) &&
             (HIGH_THRESHOLD < MINER_ID)) {
            snprintf(logBuffer, 0x800,
                     "Nonce counter overflow: chain = %d, asic = %d, nonce_return = %d, nonce_match = %d, nonce_accumulate = %d\n"
                     , nonceCounter, tempValue1, nonceReturnOffset, jobDataWord, *(undefined4 *)(NONCE_BASE_ADDRESS + nonceMatchOffset));
            LogError(3, logBuffer, 0); // FUN_0002e584 renamed to LogError
            return;
          }
          return;
        }
      }
    }
  }
  HandleInvalidData(miningDataBuffer); // FUN_000390b0 renamed to HandleInvalidData
  return;
}
```