```c
// Function to retrieve an element from a custom data structure
// param_1: Pointer to the data structure, likely representing some form of a list or array
// param_2: Index of the element to retrieve from the data structure
//
// Returns: The requested element if conditions are met, otherwise 0
uint32_t GetElementFromCustomStructure(int *structure, uint index)
{
  // Check if the pointer to the data structure is null
  if (structure == NULL) {
    return 0;
  }
  
  // Check if the data structure is initialized (first element is 1) and the requested index is valid
  if ((*structure == 1) && (index < (uint)structure[3])) {
    // Return the element at the specified index
    // structure[4] points to the start of the array of elements, while "index * 4" calculates the byte offset
    return *(uint32_t *)(structure[4] + index * 4);
  }
  
  // Return 0 if conditions are not met, indicating failure or invalid index
  return 0;
}
```