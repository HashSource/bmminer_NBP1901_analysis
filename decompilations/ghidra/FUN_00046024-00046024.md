```c
int InitializeChainIndex(uint chainIndex)
{
  int initResult;
  uint chainSpecifier;
  undefined2 command;
  undefined commandParam1;
  undefined chainID;
  
  // Check if chainIndex is within valid range (usually represents a chain on the miner)
  if (chainIndex < 0x10) {
    initResult = *(int *)(&ChainStatusBase + chainIndex * 8);
    // Check if chain is already initialized
    if ((initResult == 0) || (*(int *)(&ChainStatusBase + 4 + chainIndex * 8) == 0)) {
      command = 0; // Command code to initialize the chain
      commandParam1 = 4; // Additional parameter for the command
      chainID = (undefined)chainIndex; // Convert chainIndex to undefined type as needed by command
      chainSpecifier = chainIndex; // Copy of the chainIndex to pass as argument
      
      // Lock mutex before sending command to initialize the chain
      pthread_mutex_lock((pthread_mutex_t *)&ChainCommandMutex);
      // Send command to initialize the chain and store the result in initResult
      initResult = SendChainCommand(&chainSpecifier);
      // Unlock mutex after command is processed
      pthread_mutex_unlock((pthread_mutex_t *)&ChainCommandMutex);
      
      // If initialization was successful, update chain's status
      if (initResult >= 0) {
        *(int *)(&ChainStatusBase + chainIndex * 8) = initResult;
        *(int *)(&ChainStatusBase + 4 + chainIndex * 8) = 1;
      }
    }
  }
  else {
    // If the chainIndex is out of bounds, call error handling function
    initResult = HandleChainIndexError();
  }
  // Return the result of the initialization (positive ID or error code)
  return initResult;
}
```