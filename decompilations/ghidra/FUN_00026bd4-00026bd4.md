```c
/* WARNING: Restarted to delay deadcode elimination for space: ram */

/* Function to dump ASIC nonce information for frequency tuning */
void DumpNonceInfoForFreqTuning(undefined4 currentFreq,int nonceCountArrayBaseAddr) {

  int asicIndex;
  int chainIndex;
  undefined4 logMessageParam;
  int leftoverBufferSpace;
  int totalNonceCount;
  int tempNonceCount;
  undefined4 *nonceCountArrayPtr;
  int formattedStringLength;
  char *bufferPtr;
  char nonceInfoBuffer[260]; // Buffer to store formatted string before logging

  // Initialize the buffer with zeros
  memset(nonceInfoBuffer, 0, 0x100);
  // Check if the log object is initialized, if not then initialize it with "bmminer_tuning"
  if (logObject == 0) {
    logObject = InitializeLogObject("bmminer_tuning");
  }
  // Log the current frequency
  LogMessage(logObject, "thread.c", 8, "_tuning_freq_dump_nonce_info", 0x1c, 0x263, 0x14,
             "\n current freq: %d\n", currentFreq);

  // Loop through all chains to dump the nonce info
  chainIndex = 0; // This is the initial chain index to start from
  do {
    // Check if the current ASIC chain is valid and operational
    asicIndex = CheckIfChainIsValid(chainIndex);
    if (asicIndex != 0) {
      // Log the chain we are going to dump the nonce info from
      LogMessage(logObject, "thread.c", 8, "_tuning_freq_dump_nonce_info", 0x1c, 0x268, 0x14,
                 "Chain[%d] nonce:\n", chainIndex);

      // Get the total count of nonces for the current chain
      tempNonceCount = GetTotalNonceCount();
      if (0 < tempNonceCount) {
        nonceCountArrayPtr = (undefined4 *)(nonceCountArrayBaseAddr + -4 + chainIndex * 0x400); // Calculate the pointer to nonce count array
        totalNonceCount = 0; // Initialize total nonce count to 0
        do {
          nonceCountArrayPtr = nonceCountArrayPtr + 1; // Move to the next nonce count in the array
          tempNonceCount = totalNonceCount + 1; // Increment the temp nonce count
          // Format the nonce count for the current ASIC
          formattedStringLength = snprintf(nonceInfoBuffer + leftoverBufferSpace, 0x100 - leftoverBufferSpace,
                                           "asic[%03d]=%-8d", totalNonceCount, *nonceCountArrayPtr);
          logMessageParam = LogFormattedNonceInfo(tempNonceCount);
          RecordNonceEventInJournal(tempNonceCount, logMessageParam);
          leftoverBufferSpace = leftoverBufferSpace + formattedStringLength; // Update buffer space remaining
          if (nullTerminatorDetected == 0) { // If a null terminator is detected in snprintf output
            leftoverBufferSpace = 0; // Reset buffer space remaining
            if (logObject == 0) {
              logObject = InitializeLogObject("bmminer_tuning");
            }
            formattedStringLength = logObject;
            bufferPtr = nonceInfoBuffer;
            logMessageParam = CalculateDomain(tempNonceCount, tempNonceCount);
            // Log the nonce info for the domain calculated
            LogMessage(formattedStringLength, "thread.c", 8, "_tuning_freq_dump_nonce_info", 0x1c, 0x271, 0x14,
                       "domain %02d %s", logMessageParam, bufferPtr);
          }
          totalNonceCount = tempNonceCount; // Update the total nonce count
        } while (tempNonceCount != tempNonceCount);
      }
      // Log the total nonce count for the chain
      LogMessage(logObject, "thread.c", 8, "_tuning_freq_dump_nonce_info", 0x1c, 0x276, 0x14,
                 "Chain[%d] total nonce number = %d\n", chainIndex, 0);
      // Additional log message for the chain
      LogMessage(logObject, "thread.c", 8, "_tuning_freq_dump_nonce_info", 0x1c, 0x277, 0x14,"\n",
                 asicIndex, logMessageParam);
    }
    chainIndex = chainIndex + 1; // Move to the next chain
  } while (chainIndex != 4); // We assumed that there are 4 ASIC chains in total

  return;
}
```