```c
void UpdateMinerConfig(uint minerID, int isPartialUpdate, undefined4 configValueA, undefined4 configValueB, undefined4 configValueC,
                 undefined4 configValueD)
{
  int isMinerActive;
  uint currentMiner;

  currentMiner = 0;
  do {
    isMinerActive = CheckMinerStatus(currentMiner); // Rename FUN_0001e29c to CheckMinerStatus
    // Check if the current miner is active and matches the given miner ID or if the update is for all miners (0xff)
    if ((isMinerActive != 0) && (minerID == 0xff || currentMiner == minerID)) {
      if (isPartialUpdate == 0) {
        // Update complete configuration for the miner
        ApplyFullMinerConfig(currentMiner & 0xff, configValueA, configValueB, configValueC, (undefined2)configValueD); // Rename FUN_000505a4 to ApplyFullMinerConfig
      }
      else {
        // Apply a partial update to the miner's configuration
        ApplyPartialMinerConfig(currentMiner & 0xff, configValueB, configValueC, configValueD); // Rename FUN_00050560 to ApplyPartialMinerConfig
      }
    }
    currentMiner = currentMiner + 1;
  } while (currentMiner != 4); // Assumes there are 4 miners to check
  return;
}
```