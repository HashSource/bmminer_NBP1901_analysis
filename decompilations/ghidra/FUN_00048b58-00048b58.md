```c
void checkAndPerformInitialization(void)
{
  // Check if initialization flags are set, and if so, exit the function early.
  if ((initializationFlag1 != 0) && (initializationFlag2 != 0)) {
    return;
  }
  
  // Call a function to perform initialization if the flags are not set.
  performInitialization();
  return;
}
```