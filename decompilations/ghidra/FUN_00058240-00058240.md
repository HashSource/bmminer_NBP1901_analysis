```c
void LogRecord(int recordPointer, undefined4 logLevel)
{
  // Check if record pointer is not null
  if (recordPointer != 0) {
    // Log detailed information about the record
    Log(logLevel, "src/record.c", 0xf,
        "--record:[%p][%s:%p]--", recordPointer,
        recordPointer, *(undefined4 *)(recordPointer + 0x404));
    return;
  }
  // If record pointer is null, log an error
  Log(2, "src/record.c", 0xe, "recordPointer is null or 0");
  return;
}
```