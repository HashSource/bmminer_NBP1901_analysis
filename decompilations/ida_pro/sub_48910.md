```c
int getPowerVersion()
{
  int powerInitialized; // r5
  int statusCode; // r4
  int i2cFileDesc; // r7
  int retryCount; // r4
  int productionVersion; // r6
  int powerCode; // [sp+8h] [bp-830h] BYREF
  __int16 versionResponse; // [sp+Ch] [bp-82Ch]
  int readRequest; // [sp+10h] [bp-828h] BYREF
  int versionResponseHigh; // [sp+14h] [bp-824h]
  char logBuffer[2080]; // [sp+18h] [bp-820h] BYREF

  powerInitialized = dword_505180;
  if ( dword_505180 || (statusCode = setupPowerCommunication(), statusCode >= 0) )
  {
    i2cFileDesc = dword_50517C;
    retryCount = 10;
    do
    {
      LOBYTE(powerCode) = -1;
      LOBYTE(readRequest) = 0;
      pthread_mutex_lock(&powerI2CMutex);
      if ( i2cRead(i2cFileDesc, (unsigned __int8 *)&readRequest, 1, (int)&powerCode, 1) == 1 )
      {
        productionVersion = (unsigned __int8)powerCode;
        pthread_mutex_unlock(&powerI2CMutex);
        if ( productionVersion == 245 )
          break;
      }
      else
      {
        snprintf(logBuffer, 0x800u, "%s: iic read is failed\n", "_bitmain_get_power_code_version");
        writeToLog(0, logBuffer, 0);
        pthread_mutex_unlock(&powerI2CMutex);
      }
      usleep(0x186A0u);
      --retryCount;
    }
    while ( retryCount );
    powerCode = 33860181;
    readRequest = 0;
    versionResponseHigh = 0;
    versionResponse = 6;
    if ( i2cTransaction(i2cFileDesc, (unsigned __int8 *)&powerCode, 6u, (unsigned __int8 *)&readRequest, 8u) )
    {
      statusCode = -2147482880;
      strcpy(logBuffer, "get power version failed\n");
      writeToLog(0, logBuffer, 0);
    }
    else
    {
      statusCode = (unsigned __int16)versionResponseHigh;
      if ( (_WORD)versionResponseHigh )
        dword_505184 = (unsigned __int16)versionResponseHigh;
    }
  }
  else
  {
    snprintf(logBuffer, 0x800u, "%s: auto exec bitmain_power_open, but open power failed\n", "bitmain_power_version");
    writeToLog(powerInitialized, logBuffer, powerInitialized);
  }
  return statusCode;
}
```