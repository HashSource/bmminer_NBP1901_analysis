```c
int __fastcall CheckChainBalance(int chainIndex)
{
  int chainFunctionPtr; // r3
  char debugLogMsg[2064]; // [sp+0h] [bp-810h] BYREF

  if ( (unsigned int)globalLogLevel > 4 )
  {
    snprintf(debugLogMsg, 0x800u, "[DEBUG] Check if chain is unbalance happen, chain = %d.\n", chainIndex);
    logMessage(4, debugLogMsg, 0);
  }
  if ( chainFunctionRegistryPtr )
  {
    chainFunctionPtr = *(_DWORD *)(chainFunctionRegistryPtr + 4);
    if ( chainFunctionPtr )
      return (*(int (__fastcall **)(int))(chainFunctionPtr + 36))(chainIndex);
  }
  if ( (unsigned int)globalLogLevel > 3 )
    return LogWarningForUnbalancedChain();
  return 1;
}
```