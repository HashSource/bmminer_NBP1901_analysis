```c
int __fastcall logNoVersionError(int chainIndex)
{
  char logBuffer[2056]; // [sp+0h] [bp-808h] BYREF

  snprintf(logBuffer, sizeof(logBuffer), "No ft ver, chain = %d\n", chainIndex);
  logMessage(3, logBuffer, 0);
  return -1;
}
```