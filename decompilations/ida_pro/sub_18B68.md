```c
int logXXteaAlignmentError()
{
  char *errorMessage; // r12
  char buffer[2048]; // [sp+0h] [bp-808h] BYREF

  strcpy(errorMessage, "XXTEA requires the input as 64-bit aligned, fail to encode!\n");
  return logMessage(3, buffer, 0);
}
```