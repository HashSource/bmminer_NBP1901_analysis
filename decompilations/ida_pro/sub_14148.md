```c
int turnOffFrontFan()
{
  char logMessage[2056]; // [sp+0h] [bp-808h] BYREF

  if ( (unsigned int)globalLogLevel > 3 )
  {
    strcpy(logMessage, "Note: front fan is powered off!\n");
    logMessageToSyslog(3, logMessage, 0);
  }
  return controlFrontFan(0);
}
```