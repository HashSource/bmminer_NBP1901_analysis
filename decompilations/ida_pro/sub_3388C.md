```c
int configurePulseAndDelays()
{
  char logBuffer[2048]; // Increased the buffer size for safety

  unsigned char *pulseModeConfig = (unsigned char *)&off_7EDE0;
  unsigned char ccdlySel = (unsigned char)dword_80FAC;
  unsigned char pwthSel = (unsigned char)dword_80FA8;
  
  // Update the configuration with new `ccdly_sel` and `pwth_sel`
  pulseModeConfig[2] = ccdlySel;
  pulseModeConfig[1] = pwthSel;

  // If global log level is above 3, log the new configuration settings
  if (globalLogLevel > 3)
  {
    snprintf(
      logBuffer,
      sizeof(logBuffer),
      "pulse_mode = %d, ccdly_sel = %d, pwth_sel = %d\n",
      pulseModeConfig[0],
      ccdlySel,
      pwthSel);
    logMessage(3, logBuffer, 0);
  }

  // Return the new configuration as a 24-bit integer
  return pulseModeConfig[0] | (pulseModeConfig[1] << 8) | (pulseModeConfig[2] << 16);
}
```