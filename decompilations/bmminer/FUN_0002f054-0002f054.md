```c
void updateBufferWithInputData(int *bufferInfo, void *inputData, uint inputSize) {
  uint spaceRemaining;

  // Calculate available space in the buffer
  spaceRemaining = 0x40 - bufferInfo[1];
  
  // Copy only as much data as will fit in the remaining space
  if (inputSize <= spaceRemaining) {
    spaceRemaining = inputSize;
  }

  // Perform the copy of data into the buffer
  memcpy((void *)((int)bufferInfo + bufferInfo[1] + 8), inputData, spaceRemaining);

  // Check if there is still space left in the buffer after copying
  if (inputSize + bufferInfo[1] < 0x40) {
    bufferInfo[1] += inputSize; // Update size within buffer
    return;
  }

  // Calculate the remaining size of input data to be processed
  inputSize -= spaceRemaining;
  
  // Process the buffer data 
  processBufferData(bufferInfo, bufferInfo + 2, 1);

  // Process additional data chunks if inputSize is large enough
  processBufferData(bufferInfo, (int)inputData + spaceRemaining, inputSize >> 6);

  // Copy any remaining input data into the buffer
  memcpy(bufferInfo + 2, (void *)((int)inputData + spaceRemaining + (inputSize & 0xffffffc0)), inputSize & 0x3f);

  // Update the size within the buffer with the remaining input data size
  bufferInfo[1] = inputSize & 0x3f;

  // Update the total processed size in the buffer
  *bufferInfo = *bufferInfo + ((inputSize >> 6) + 1) * 0x40;

  return;
}
```