```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>

// Global variables to store the debug and error log file paths
// Initialized as zero and will be set once by calling the below function
int isLogFilePathInitialized = 0;
char *debugLogFilePath = NULL;
char *errorLogFilePath = NULL;

// Function prototypes for clarity
void getFormattedTime(char *buffer);
undefined4 logMessageToConsole(int logLevel, undefined4 messageParam1, undefined4 messageParam2, char *formatString);

// Log levels for easy reference
enum LogLevel { DEBUG = 0, WARNING = 1, ERROR = 2 };

// Function to log messages to their corresponding files
undefined4 logMessage(int logLevel, undefined4 messageParam1, undefined4 messageParam2, char *formatString) {
  __pid_t processId;
  FILE *logFile;
  char timeBuffer[24];
  
  // Initialize the log file paths once
  if (isLogFilePathInitialized == 0) {
    isLogFilePathInitialized = 1;
    debugLogFilePath = getenv("ZLOG_PROFILE_DEBUG");
    errorLogFilePath = getenv("ZLOG_PROFILE_ERROR");
  }

  // Handle log message based on the specified log level
  switch (logLevel) {
    case WARNING:
      if (errorLogFilePath == NULL) {
        return 0;
      }
      logFile = fopen64(errorLogFilePath, "a");
      if (logFile == NULL) {
        return 0xffffffff;
      }
      getFormattedTime(timeBuffer);
      processId = getpid();
      fprintf(logFile, "%s WARN  (%d:%s:%ld) ", timeBuffer, processId, messageParam1, messageParam2);
      break;
      
    case ERROR:
      if (errorLogFilePath == NULL) {
        return 0;
      }
      logFile = fopen64(errorLogFilePath, "a");
      if (logFile == NULL) {
        return 0xffffffff;
      }
      getFormattedTime(timeBuffer);
      processId = getpid();
      fprintf(logFile, "%s ERROR (%d:%s:%ld) ", timeBuffer, processId, messageParam1, messageParam2);
      break;
      
    case DEBUG:
      if (debugLogFilePath == NULL) {
        return 0;
      }
      logFile = fopen64(debugLogFilePath, "a");
      if (logFile == NULL) {
        return 0xffffffff;
      }
      getFormattedTime(timeBuffer);
      processId = getpid();
      fprintf(logFile, "%s DEBUG (%d:%s:%ld) ", timeBuffer, processId, messageParam1, messageParam2);
      break;
      
    default:
      logFile = NULL;
  }

  // If there's a logFile opened, write the formatted message to it
  if (logFile != NULL) {
    vfprintf(logFile, formatString, &stack0x00000000); // Passed the vararg to formatted message
    fputc('\n', logFile); // Adding a newline at the end of the log message
    fclose(logFile); // Close the file stream to save the message
  }

  return 0;
}
```