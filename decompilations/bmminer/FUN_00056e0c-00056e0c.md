```c
void FreeConfigurationMemory(void *configMemoryPtr)
{
  // Check if the configuration memory pointer is not null
  if (configMemoryPtr != NULL)  {
    // If the feature at offset 0x1428 is enabled, call the corresponding function to handle it
    if (*(int *)((int)configMemoryPtr + 0x1428) != 0) {
      ReleaseFeature1428Resources();
    }
    // If the feature at offset 0x2440 is enabled, call the corresponding function to handle it
    if (*(int *)((int)configMemoryPtr + 0x2440) != 0) {
      ReleaseFeature2440Resources();
    }
    // If the feature at offset 0x2430 is enabled, call the corresponding function to handle it
    if (*(int *)((int)configMemoryPtr + 0x2430) != 0) {
      ReleaseFeature2430Resources();
    }
    // If the feature at offset 0x2444 is enabled, call the corresponding function to handle it
    if (*(int *)((int)configMemoryPtr + 0x2444) != 0) {
      ReleaseFeature2444Resources();
    }
    // If the feature at offset 0x2448 is enabled, call the corresponding function twice to handle it
    if (*(int *)((int)configMemoryPtr + 0x2448) != 0) {
      ReleaseFeature2448Resources();
      ReleaseFeature2448Resources();
    }
    // Free the allocated memory for the configuration
    free(configMemoryPtr);
    // Log that the configuration has been deleted
    LogMessage(0, "src/conf.c", 0x5a, "zlog_conf_del[%p]");
    return;
  }
  // Log an error if the passed configuration memory pointer is null
  LogMessage(2, "src/conf.c", 0x53, "a_conf is null or 0");
  return;
}
```