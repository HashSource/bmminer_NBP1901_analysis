```c
void processInputData(uint input_val, undefined4 arg2, undefined4 arg3, undefined4 arg4)
{
  uint *input_ptr;
  uint *output_ptr;
  uint *temp_ptr;
  int index;
  uint input_buffer[5]; // 20 bytes buffer as 5 x 4-byte integers
  uint output_buffer[8]; // 32 bytes buffer as 8 x 4-byte integers
  uint stack_local_10;
  undefined4 stack_local_c;
  undefined4 stack_local_8;
  undefined4 stack_local_4;
  
  stack_local_10 = input_val;
  stack_local_c = arg2;
  stack_local_8 = arg3;
  stack_local_4 = arg4;
  // Initialize buffer with zeros starting from input_buffer[2], for 0x2c bytes (44 bytes)
  memset(input_buffer + 2, 0, 0x2c);
  index = 2; // Initialize index to start filling the input buffer from input_buffer[2]
  
  // Byte-swap and store the input_val into input_buffer[0]
  input_buffer[0] =
       input_val << 0x18 | (input_val >> 8 & 0xff) << 0x10 | (input_val >> 0x10 & 0xff) << 8 |
       input_val >> 0x18;
  
  // Store the second argument into input_buffer[1]
  input_buffer[1] = stack_local_c;
  
  // Process remaining arguments and store them in input_buffer starting from index 2
  input_ptr = &stack_local_10;
  do {
    input_buffer[index] =
         (uint)*(byte *)((int)input_ptr + 9) << 0x10 | (uint)*(byte *)(input_ptr + 2) << 0x18 |
         (uint)*(byte *)((int)input_ptr + 0xb) | (uint)*(byte *)((int)input_ptr + 10) << 8;
    index += 1;
    input_ptr += 1;
  } while (index != 5);
  
  // Process and store values in the output_buffer
  output_ptr = output_buffer;
  input_ptr = &stack_local_10;
  do {
    temp_ptr = input_ptr + 1;
    *output_ptr = (uint)*(byte *)((int)input_ptr + 0x15) << 0x10 | (uint)*(byte *)(input_ptr + 5) << 0x18 |
                  (uint)*(byte *)((int)input_ptr + 0x17) | (uint)*(byte *)((int)input_ptr + 0x16) << 8;
    output_ptr += 1;
    input_ptr = temp_ptr;
  } while (temp_ptr != (uint *)&stack_local_10);
  
  // Call some function to further process the input buffer
  someFunction(input_buffer, 0x34); // 0x34 is 52 in decimal, could be the size of data to process
  return;
}
```