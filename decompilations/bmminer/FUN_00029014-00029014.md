```c
void LogMutexError(undefined4 fileName, undefined4 functionName, undefined4 lineNumber)
{
  int *pErrorNumber;
  char errorMsgBuffer[2052];
  
  pErrorNumber = __errno_location(); // Get the location of the error number
  // Create the error message string with information about where the mutex error occurred
  snprintf(errorMsgBuffer, 0x800, "WTF MUTEX ERROR ON LOCK! errno=%d in %s %s():%d",
           *pErrorNumber, "bmminer.c", fileName, functionName, lineNumber);
  // Log the error message to the appropriate log with error level 3
  LogError(3, errorMsgBuffer, 1);
  return;
}
```