```c
// Defining meaningful names for the magic numbers used in the switch case statement
#define DEVICE_TYPE_A 0x41
#define DEVICE_TYPE_B 0x42
#define DEVICE_TYPE_C 0x43
#define DEVICE_TYPE_D 0x71
#define DEVICE_TYPE_E 0x72
#define DEVICE_TYPE_F 0x74
#define DEVICE_TYPE_G 0x75
#define DEVICE_TYPE_H 0x76
#define DEVICE_TYPE_I 0x77

// Global variables with more descriptive names indicating their roles (placeholders for actual names)
double globalTemperatureCalibrationOffsetA;  // Assuming these are related to temperature calibration
double globalTemperatureCalibrationFactorA;  // DAT_00048ed0 renamed
double globalTemperatureCalibrationOffsetB;
double globalTemperatureCalibrationFactorB;  // DAT_00048ee0 renamed
double globalTemperatureCalibrationOffsetC;
double globalTemperatureCalibrationFactorC;  // DAT_00048ec0 renamed
double globalTemperatureCalibrationOffsetD;
double globalTemperatureCalibrationFactorD;  // DAT_00048ef0 renamed

// Global variable that dictates the type of device, to decide on calibration parameters
unsigned char deviceTypeCode; // DAT_00505184 renamed

// Function to calculate adjusted temperature based on device type and other factors
int calculateTemperatureAdjustment(void)
{
  double tempMeasurement;
  int temperatureAdjustment; // Variable to hold the calculated temperature adjustment value
  
  // based on the current device type
  switch(deviceTypeCode) {
    case DEVICE_TYPE_A:
    case DEVICE_TYPE_B:
      // Calculate adjustment for device types A and B
      temperatureAdjustment = (int)(long long)(globalTemperatureCalibrationOffsetA - tempMeasurement * globalTemperatureCalibrationFactorA);
      break;
    case DEVICE_TYPE_C:
      // Calculate adjustment for device type C
      temperatureAdjustment = (int)(long long)(globalTemperatureCalibrationOffsetB - tempMeasurement * globalTemperatureCalibrationFactorB);
      break;
    case DEVICE_TYPE_D:
    case DEVICE_TYPE_E:
    case DEVICE_TYPE_G:
    case DEVICE_TYPE_I:
      // Calculate adjustment for device types D, E, G, and I
      temperatureAdjustment = (int)(long long)(globalTemperatureCalibrationOffsetC - tempMeasurement * globalTemperatureCalibrationFactorC);
      break;
    case DEVICE_TYPE_F:
    case DEVICE_TYPE_H:
      // Calculate adjustment for device types F and H
      temperatureAdjustment = (int)(long long)(globalTemperatureCalibrationOffsetD - tempMeasurement * globalTemperatureCalibrationFactorD);
      break;
    default:
      // Default case when device type is unknown or unsupported
      temperatureAdjustment = -1;
      break;
  }
  return temperatureAdjustment;
}
```