```c
/* WARNING: Globals starting with '_' overlap smaller symbols at the same address */

// Function to release memory used for hardware sweeping
void ReleaseHwSweepMemory(void)
{
  // Declare local variables to store debug messages
  undefined4 debugMessagePart1;
  undefined4 debugMessagePart2;
  undefined4 debugMessagePart3;
  undefined4 debugMessagePart4;
  undefined4 debugMessagePart5;
  undefined4 debugMessagePart6;
  undefined4 debugMessagePart7;
  undefined4 debugMessagePart8;
  undefined2 debugMessagePart9;
  undefined debugMessagePart10;

  // Check if the log level is greater than 4 (DEBUG level)
  if (4 < globalLogLevel) {
    // Copy debug message parts from global variables
    debugMessagePart1 = globalDebugMessage._0_4_;
    debugMessagePart2 = globalDebugMessage._4_4_;
    debugMessagePart3 = globalDebugMessage._8_4_;
    debugMessagePart4 = globalDebugMessage._12_4_;
    debugMessagePart5 = globalDebugMessage._16_4_;
    debugMessagePart6 = globalDebugMessage._20_4_;
    debugMessagePart7 = globalDebugMessage._24_4_;
    debugMessagePart8 = globalDebugMessage._28_4_;
    debugMessagePart9 = (undefined2)globalDebugMessagePostfix;
    debugMessagePart10 = (undefined)((uint)globalDebugMessagePostfix >> 0x10);

    // Log the debug message
    LogDebugMessage(4, &debugMessagePart1, 0);
  }

  // Check if the global pointer to the sweep memory structure is not NULL
  if (globalSweepMemoryPtr != (void *)0x0) {
    // Check if the second pointer within the sweep memory structure is not NULL
    if (*(void **)((int)globalSweepMemoryPtr + 4) != (void *)0x0) {
      // Free the memory pointed to by the second pointer
      free(*(void **)((int)globalSweepMemoryPtr + 4));
    }
    // Free the memory pointed to by the first (top-level) pointer
    free(globalSweepMemoryPtr);
    // Set the global pointer to NULL after freeing memory
    globalSweepMemoryPtr = (void *)0x0;
  }

  // Function return (void)
  return;
}
```