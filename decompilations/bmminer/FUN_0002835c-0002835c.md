```c
#define ERROR_CODE_SUCCESS 0

// Global variable holding some data, possibly an error message or a constant value.
extern undefined4 GLOBAL_DATA_00028370;

// Function that likely initializes or sets up a resource with provided data.
// The data used for initialization is taken from GLOBAL_DATA_00028370.
extern void InitializeResourceWithGlobalData(undefined4 resource, undefined4 globalData);

/**
 * Function to setup a specified resource using predefined global data.
 * 
 * param_1: The identifier or handle for the resource to initialize.
 * 
 * Returns: Always returns ERROR_CODE_SUCCESS (0).
 */
undefined4 SetupResourceWithPredefinedData(undefined4 resourceIdentifier)
{
  InitializeResourceWithGlobalData(resourceIdentifier, GLOBAL_DATA_00028370);
  return ERROR_CODE_SUCCESS;
}
```
