```c
int performSensorCalibration(uint32_t calibrationValue)
{
  int chainIndex;
  int calibrationResult;
  char logBuffer[2048];
  
  int calibrationStatus = checkCalibrationAvailability();
  if (calibrationStatus != 0) {
    chainIndex = 0;
    pthread_mutex_lock((pthread_mutex_t *)&calibrationMutex);
    globalCalibrationValue = calibrationValue;
    triggerCalibrationProcess();
    while ((calibrationResult = verifyCalibration(chainIndex), calibrationResult == 0 || 
           (calibrationResult = attemptCalibration(chainIndex), calibrationResult == 0))) {
      chainIndex = chainIndex + 1;
      if (chainIndex == 4) {
        calibrationReadyFlag = 1;
        pthread_mutex_unlock(&mutexForCalibration);
        return 0;
      }
    }
    if (chainIndex > maxChainCount) {
      snprintf(logBuffer, 0x800, "calibration sensor on chain %d failed", chainIndex);
      logError(3, logBuffer, 0);
    }
    calibrationReadyFlag = 1;
    pthread_mutex_unlock(&mutexForCalibration);
    chainIndex = -1;
  }
  return chainIndex;
}
```

Comments explaining the code:

```c
// Function to perform sensor calibration on the mining hardware.
// Takes a calibration value as input and returns the result of calibration.
int performSensorCalibration(uint32_t calibrationValue)
{
  int chainIndex; // Index to iterate through the mining hardware chains.
  int calibrationResult;
  char logBuffer[2048]; // Buffer for logging messages.
  
  // First, checks if calibration can be performed.
  int calibrationStatus = checkCalibrationAvailability();
  if (calibrationStatus != 0) {
    chainIndex = 0; // Start from the first chain.
    // Locks the calibration process to ensure thread safety.
    pthread_mutex_lock((pthread_mutex_t *)&calibrationMutex);

    // Sets the global calibration value.
    globalCalibrationValue = calibrationValue;
    // Triggers the calibration process.
    triggerCalibrationProcess();

    // Continues to verify and attempt calibration on each chain until all are calibrated.
    while ((calibrationResult = verifyCalibration(chainIndex), calibrationResult == 0 || 
           (calibrationResult = attemptCalibration(chainIndex), calibrationResult == 0))) {
      chainIndex = chainIndex + 1;
      // If all chains have been checked, mark calibration as ready and return success.
      if (chainIndex == 4) {
        calibrationReadyFlag = 1;
        pthread_mutex_unlock(&mutexForCalibration);
        return 0;
      }
    }
    // If the calibration failed on any chain, log the error.
    if (chainIndex > maxChainCount) {
      snprintf(logBuffer, 0x800, "calibration sensor on chain %d failed", chainIndex);
      logError(3, logBuffer, 0);
    }
    // Set the ready flag and unlock the mutex.
    calibrationReadyFlag = 1;
    pthread_mutex_unlock(&mutexForCalibration);
    chainIndex = -1; // Use -1 to indicate an error occurred.
  }
  // Return the status of the calibration process.
  return chainIndex;
}
```