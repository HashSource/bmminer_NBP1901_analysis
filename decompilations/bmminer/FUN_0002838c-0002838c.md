```c
void DisplayMiningSupportInfo(undefined4 firmwareVersion)
{
  undefined4 supportMessage;
  
  // Print the build information including the firmware version
  printf("%s\nBuilt with Bitmain Antminer C5 mining support.\n", firmwareVersion);
  
  // Get the support message using a previously defined function and print it
  supportMessage = GetSupportMessage(GlobalSupportData, firmwareVersion);
  printf("%s", supportMessage);
  
  // Flush the stdout buffer to ensure the message is printed immediately
  fflush(stdout);
  
  // Exit the program
  exit(0);
}
```