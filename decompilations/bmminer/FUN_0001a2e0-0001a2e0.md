```c
#define MODE_INDEX 2 // Assuming DAT_0007edc8 refers to some system mode index

// Presuming these data sections are some kind of lookup tables or configuration settings
// The actual addresses for these would be determined by analyzing the full binary.
extern int CONFIG_TABLE_A[];
extern int CONFIG_TABLE_B[];

// Function to get configuration value based on system mode and a parameter index
int GetConfigurationValue(int parameterIndex) {
  int configValue;

  if (MODE_INDEX == 2) {
    // If the system mode index is 2, use values from CONFIG_TABLE_A
    configValue = CONFIG_TABLE_A[parameterIndex];
  } else {
    // Otherwise, use values from CONFIG_TABLE_B
    configValue = CONFIG_TABLE_B[parameterIndex];
  }

  return configValue;
}
```