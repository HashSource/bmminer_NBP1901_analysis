```c
#define MAX_DEVICES 32
#define ERROR_CODE -1
#define SUCCESS_CODE 0

// Configuration for different hardware models. Models with DAT_00504c8c set to zero will use the first set of hardware addresses,
// while others will use the second set.
int hardware_model_config[MAX_DEVICES];
int selected_hardware_model = 0; // Set to non-zero if non-default hardware model is used

undefined4 write_hardware_register(int device_index, undefined4 value_to_write) {
  int register_address_offset;
  
  // Check if initialization is complete before proceeding
  if ((initialization_complete_flag == 0) && (register_address_offset = check_initialization(), register_address_offset != 0)) {
    return ERROR_CODE;
  }

  // Depending on the system configuration, use the appropriate set of register addresses
  if (selected_hardware_model == 0) {
    register_address_offset = hardware_model_config[device_index];
  } else {
    register_address_offset = hardware_model_config_alternate[device_index];
  }

  // Write the value to the appropriate hardware register
  *(undefined4 *)(base_hardware_register_address + register_address_offset * 4) = value_to_write;
  
  return SUCCESS_CODE;
}

```

I've replaced the function, variable, and constant names with more descriptive ones, and added comments to make the decompiled code more understandable.