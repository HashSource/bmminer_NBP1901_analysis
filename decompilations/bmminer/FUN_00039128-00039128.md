```c
/* WARNING: Renaming symbols can overlap smaller symbols at the same address */

void ExecuteMiningTask(undefined4 taskData) {
  undefined4 debugMessageBuffer1;
  undefined4 debugMessageBuffer2;
  undefined4 debugMessageBuffer3;
  undefined4 debugMessageBuffer4;
  char debugMessageSuffix;

  // Check if global mining handle is set and the corresponding function is non-null
  if ((globalMiningHandle == 0) || (*(int *)(globalMiningHandle + 4) == 0)) {
    if (verbosityLevel > 3) {
      // Prepare a debug message indicating that the handle is NULL
      debugMessageBuffer1 = "Handle is NULL__"._0_4_;
      debugMessageBuffer2 = "Handle is NULL__"._4_4_;
      debugMessageBuffer3 = "Handle is NULL__"._8_4_;
      debugMessageBuffer4 = "Handle is NULL__"._12_4_;
      debugMessageSuffix = (char)ram0x00067bd8;
      // Log the debug message
      LogDebugMessage(3, &debugMessageBuffer1, 0);
      return;
    }
  } else {
    // Lock the mining mutex before modifying any global state
    pthread_mutex_lock((pthread_mutex_t *)&globalMiningMutex);
    
    // Check if the initialized flag is 0, which might indicate initialization needed
    if (*(int *)(globalMiningHandle + 0x40) == 0) {
      InitializeMiningTask(taskData); // Custom function to initialize task
    } else {
      // Call the mining function associated with the global handle
      (*(void (**)(undefined4))( *(int *)(globalMiningHandle + 4) + 0x18))(taskData);
    }
    // Release the mining mutex
    pthread_mutex_unlock((pthread_mutex_t *)&globalMiningMutex);
  }
  return;
}
```