```c
char * GetNextNonOptionArgument(undefined4 fileDescriptor)
{
  char *argumentPtr;
  undefined4 bytesRead[2];
  
  bytesRead[0] = 0;
  argumentPtr = (char *)ReadNextArgument(fileDescriptor, bytesRead);
  while( true ) {
    if (argumentPtr == NULL) {
      // If no argument is left, return NULL
      return NULL;
    }
    // Check if the argument is an option (starts with '-')
    if (*argumentPtr != '-') break;
    
    // Read the next argument if the current one is an option
    argumentPtr = (char *)SkipOptionArgument(argumentPtr, fileDescriptor, bytesRead);
  }
  // Return the pointer to the non-option argument
  return argumentPtr;
}
```