```c
void UnlockLoggingMutexWithErrorCheck(undefined4 threadIdentifier)
{
  int unlockResult;
  int *errNumPtr;
  char errorMessageBuffer[2048];
  
  // Attempt to unlock the logging mutex
  unlockResult = pthread_mutex_unlock((pthread_mutex_t *)&LoggingMutex);
  
  // If unlocking succeeded, simply return
  if (unlockResult == 0) {
    return;
  }
  
  // If an error occurred, fetch the error number
  errNumPtr = __errno_location();
  
  // Compose an error message with details about the failure
  snprintf(errorMessageBuffer,0x800,"WTF MUTEX ERROR ON UNLOCK! errno=%d in %s %s():%d",
           *errNumPtr, "logging.c", FunctionName, threadIdentifier);
  
  // Log the error message with a severity level of 3 (error level)
  LogError(3, errorMessageBuffer, 1);
  
  // Terminate the program with error status
  TerminateProgram(1);
  
  return;
}
```