```c
void ShutdownControlTasks(void)
{
  // Check if control task 1 is running, indicated by a non-zero global variable
  if (globalControlTask1Running != 0) {
    // Stop control task 1
    StopControlTask1();
  }
  // Mark control task 1 as stopped
  globalControlTask1Running = 0;
  // Reset related control variable for task 1
  globalControlTask1Variable = 0;
  
  // Check if control task 2 is running, indicated by a non-zero global variable
  if (globalControlTask2Running != 0) {
    // Stop control task 2
    StopControlTask2();
  }
  // Mark control task 2 as stopped
  globalControlTask2Running = 0;

  // Check if control task 3 is running, indicated by a non-zero global variable
  if (globalControlTask3Running != 0) {
    // Stop control task 3
    StopControlTask3();
  }
  // Mark control task 3 as stopped
  globalControlTask3Running = 0;

  return;
}
```