```c
void LogRuleDetails(int rule_ptr, undefined4 log_level) {
  int index;
  int *rule_data_ptr;
  int array_length;
  
  // If the rule pointer is not null, proceed to log its details.
  if (rule_ptr != 0) {
    LogFormatted(
      log_level, 
      "src/rule.c",
      0x41,
      "---rule:[%p][%s%c%d]-[%d,%d][%s,%p,%d:%ld*%d~%s][%d][%d][%s:%s:%p];[%p]---",
      rule_ptr,
      rule_ptr,
      *(undefined *)(rule_ptr + 0x1001), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1004), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1028), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x102c), // Placeholder for actual structure member name
      rule_ptr + 0x1030, // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1434), // Pointer to an array or list
      *(undefined4 *)(rule_ptr + 0x1438), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1450), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1454), // Placeholder for actual structure member name
      rule_ptr + 0x1458, // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1864), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1874), // Placeholder for actual structure member name
      rule_ptr + 0x1880, // Placeholder for actual structure member name
      rule_ptr + 0x1c81, // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x2084), // Placeholder for actual structure member name
      *(undefined4 *)(rule_ptr + 0x1878) // Placeholder for actual structure member name
    );

    // Get pointer to an array or list within the rule data.
    rule_data_ptr = *(int **)(rule_ptr + 0x1434);
    // Check if the array pointer is non-null and has a positive length.
    if ((rule_data_ptr != NULL) && (0 < rule_data_ptr[1])) {
      array_length = 0;
      do {
        index = array_length * 4;
        array_length = array_length + 1;
        LogIndividualItemDetails(*(undefined4 *)(*rule_data_ptr + index), log_level);
        rule_data_ptr = *(int **)(rule_ptr + 0x1434);
      } while (array_length < rule_data_ptr[1]);
    }
    return;
  }

  // If the rule pointer is null, log an error message indicating a null rule.
  LogFormatted(2, "src/rule.c", 0x27, "a_rule is null or 0");
  return;
}
```