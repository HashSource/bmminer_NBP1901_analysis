```c
int GetHashtableValue(undefined4 *hashtablePointer)
{
  int getValueResult;
  
  getValueResult = RetrieveValueFromHashtable(*hashtablePointer);
  if (getValueResult != 0) {
    return getValueResult + 0x401; // Add a specific offset to the retrieved value before returning
  }
  LogError(2,"src/mdc.c",0x79,"zc_hashtable_get fail"); // Log an error with a message "zc_hashtable_get fail"
  return 0;
}
```