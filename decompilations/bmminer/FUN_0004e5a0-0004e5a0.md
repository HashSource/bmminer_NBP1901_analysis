```c
int GetMiningStatus(int *statusArray)
{
  int status = 0;
  
  if (statusArray != NULL) { // Check if the pointer to the status array is not NULL
    if (*statusArray == 1) { // Check if the first element indicates a status of '1', which might mean 'mining'
      status = statusArray[3]; // Get the value at the fourth position of the array, could be specific status info
    }
    else {
      // If the first element is not '1', set status to '0' (probably indicating not mining or idle state)
      status = 0;
    }
    return status; // Return the retrieved status
  }
  // If the status array pointer is NULL, return '0'
  return 0;
}
```