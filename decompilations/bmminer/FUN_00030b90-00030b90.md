```c
#include <pthread.h>

// Function prototype for task initialization
typedef void (*TaskInitFunc)(int arg);

// TaskThreadRoutine - The routine for a task thread
// param_1: An integer pointer that points to the task-related data
// The function signature and types are undefined4 and int due to decompilation
// but the actual types might differ in the original source code.
unsigned int TaskThreadRoutine(int *taskDataPtr)
{
  // Set the thread's cancelability type to asynchronous
  pthread_setcanceltype(PTHREAD_CANCEL_ASYNCHRONOUS, NULL);

  // Retrieve the TaskInitFunc function pointer from the task data structure
  // and call the function passing the argument also from the task data structure
  TaskInitFunc initialFunc = *(TaskInitFunc *)(taskDataPtr + 4); // Assuming + 0x10 offset corresponds to 4*sizeof(int)
  int arg = *(int *)(taskDataPtr + 5); // Assuming + 0x14 offset corresponds to 5*sizeof(int)
  initialFunc(arg);

  // Call to a logging function with "util.c" as the context,
  // and a magic number (DAT_00030bd0) possibly a line number or error code
  // 0xe36 is another constant that could be an error code or line number
  LogTaskEvent(taskDataPtr, "util.c", MAGIC_NUMBER, 0xe36);

  // Return 0 indicating success or normal termination
  return 0;
}

// This is a placeholder for the actual logging function since its implementation or purpose is not clear
// however, it seems to take some task-related data, a file name, a numeric constant, and another number,
// possibly for log categorization or error tracking purposes.
void LogTaskEvent(int *taskDataPtr, const char *sourceFile, int magicNumber, int lineNumber)
{
  // Original logic for the logging function should be present here
  // Since the original behavior isn't provided, this is a blank implementation
}
```