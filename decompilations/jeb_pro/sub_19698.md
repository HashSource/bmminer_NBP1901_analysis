```c
int checkAndAdjustVoltage(int chainIndex, int targetVoltage, size_t dataSize) {
    __int128 intermediateValue;
    int status;
    short v1_short;
    int v2_int;
    int v3_int;
    int v4_int;
    int v5_int;
    int v6_int;
    int logBuffer;
    char v7_char;
    int comparisonResult;
    short v9_short;
    short v10_short;
    double floatComparison;
    int64_t int64Temporary;
    int chainIndexLocal = chainIndex;
    int powerVersion = getPowerVersion();
    if(globalDebugLevel > 3) {
        snprintf(&logBuffer, 0x800, "power type version: 0x%04x\n", powerVersion);
        logDebugMessage(3, &logBuffer, 0);
    }
    int powerInitStatus = initializePower((int)(uint16_t)powerVersion);
    int powerInitResult = powerInitStatus;
    uint32_t debugLevel = globalDebugLevel;
    if(powerInitStatus == 0) {
        if(debugLevel > 3) {
            logBuffer = 0x65776f70; // "power"
            v6_int = 2037653618;
            v5_int = 0x76206570; // "ep v"
            v4_int = 1769173605;
            v3_int = 1696624239;
            v2_int = 0x726f7272; // "rror"
            v1_short = 10;
            logDebugMessage(3, &logBuffer, powerInitResult);
        }
        return -1;
    }
    if(debugLevel > 3) {
        logBuffer = 1702129221;
        v6_int = 1819484274;
        v5_int = 0x20706565;
        v4_int = 1830842228;
        v3_int = 543517537;
        v2_int = 1701999987;
        *(int*)&v1_short = 0x776f7020; // " pow"
        *(int*)&v9_short = 0x72207265; // "er re"
        *(int*)&v7_char = 'e';
        *(int*)&v10_short = 1713399155;
        int v18_int = 0x73696e69; // "sini"
        int v19_int = 667240;
        powerInitStatus = logDebugMessage(3, &logBuffer, 0);
    }
    writePowerValue(powerInitStatus, targetVoltage, dataSize);
    sleep(1);
    resetDevice();
    int readResult = readVoltage(chainIndexLocal);
    status = readResult;
    if(status >= 0) {
        readPowerValue(status, targetVoltage, dataSize);
        int adjustedVersion = powerVersion - 117;
        sleep(1);
        char flag_lt_3 = adjustedVersion < 3;
        char flag_eq_3 = adjustedVersion == 3;
        char flag_sign = (((adjustedVersion - 3) ^ adjustedVersion) & (adjustedVersion ^ 0x3)) < 0;
        char flag_ge_3 = (uint32_t)adjustedVersion >= 3;
        if(flag_ge_3 != 0 && flag_eq_3 == 0) {
            intermediateValue = (unsigned __int128)(intermediateValue & 0xFFFFFFFFFFFFFFFFFFFFFFFFX) | ((unsigned __int128)chainIndexLocal << 96);
            int retryCount = 3;
            convertIntToFloat64(int64Temporary, (uint32_t)(intermediateValue >>> 0x60X));
            do {
                sleep(1);
                resetASIC();
                multiplyFloat64((uint64_t)intermediateValue, int64Temporary, 0x3fe8000000000000L);
                divideFloat64((uint64_t)(intermediateValue >>> 0x40X), (uint64_t)intermediateValue, 0x4059000000000000L);
                compareFloat64(floatComparison, (uint64_t)(intermediateValue >>> 0x40X));
                moveFloatFlags((uint8_t)flag_sign | ((uint8_t)flag_ge_3 << SOCK_STREAM) | ((uint8_t)flag_eq_3 << 2) | ((uint8_t)flag_lt_3 << 3), comparisonResult);
                if(flag_lt_3 == 0) {
                    return 0;
                }
                if(globalDebugLevel > 3) {
                    logBuffer = 0x65776f70;
                    v6_int = 1870012530;
                    v5_int = 1734440044;
                    v4_int = 1633886309;
                    v3_int = 0x6f6e206e;
                    v2_int = 0x656d2074;
                    *(int*)&v1_short = 1948284005;
                    *(int*)&v9_short = 0x74206568;
                    __builtin_strcpy(&v7_char, "arget");
                    logDebugMessage(3, &logBuffer, 0);
                }
                int retryTemp = retryCount;
                --retryCount;
                flag_lt_3 = retryTemp < SOCK_STREAM;
                flag_eq_3 = retryTemp == SOCK_STREAM;
                flag_sign = (((retryTemp - SOCK_STREAM) ^ retryTemp) & (retryTemp ^ SOCK_STREAM)) < 0;
                flag_ge_3 = retryTemp >= SOCK_STREAM;
            }
            while(retryCount != 0);
            return -1;
        }
        int checkCount = 3;
        while(SOCK_STREAM) {
            sleep(1);
            resetErrorLatch(0);
            compareFloat64ToZero(floatComparison);
            moveFloatFlags((uint8_t)flag_sign | ((uint8_t)flag_ge_3 << SOCK_STREAM) | ((uint8_t)flag_eq_3 << 2) | ((uint8_t)flag_lt_3 << 3), comparisonResult);
            if(flag_lt_3 == 0) {
                intermediateValue = (unsigned __int128)(intermediateValue & 0xFFFFFFFFFFFFFFFFFFFFFFFFX) | ((unsigned __int128)chainIndexLocal << 96);
                multiplyFloat64Into(floatComparison, floatComparison, 0x4059000000000000L);
                convertFloat64ToInt32((uint64_t)(intermediateValue >>> 0x40X), (uint32_t)(intermediateValue >>> 0x60X));
                multiplyFloat64((uint64_t)intermediateValue, (uint64_t)(intermediateValue >>> 0x40X), 0x3ff199999999999aL);
                compareFloat64(floatComparison, (uint64_t)intermediateValue);
                moveFloatFlags((uint8_t)flag_sign | ((uint8_t)flag_ge_3 << SOCK_STREAM) | ((uint8_t)flag_eq_3 << 2) | ((uint8_t)0 << 3), comparisonResult);
                if(flag_eq_3 == 0 && flag_sign == 0) {
                    uint32_t debugStatus = globalDebugLevel;
                    char flag_debug_lt_3 = (int)debugStatus < 3;
                    char flag_debug_eq_3 = debugStatus == 3;
                    char flag_debug_sign = (((debugStatus - 3) ^ debugStatus) & (debugStatus ^ 0x3)) < 0;
                    char flag_debug_ge_3 = debugStatus >= 3;
                    if((flag_debug_ge_3 != 0 && flag_debug_eq_3 == 0)) {
                        intermediateValue = (unsigned __int128)(uint64_t)intermediateValue | ((unsigned __int128)(floatComparison + (uint64_t)(intermediateValue >>> 0x40X)) << 64);
                        compareFloat64((uint64_t)(intermediateValue >>> 0x40X), 0);
                        moveFloatFlags((uint8_t)flag_debug_sign | ((uint8_t)flag_debug_ge_3 << SOCK_STREAM) | ((uint8_t)flag_debug_eq_3 << 2) | ((uint8_t)flag_debug_lt_3 << 3), comparisonResult);
                        snprintf(&logBuffer, 0x800, "chain avg vol %s from %d to %.2f", flag_debug_eq_3 == 0 && flag_debug_sign == flag_debug_lt_3 ? "rise": "drop", chainIndexLocal);
                        logDebugMessage(3, &logBuffer, 0);
                        if(globalDebugLevel > 3) {
                            logBuffer = 0x65776f70; // "power"
                            v6_int = 1870012530;
                            v5_int = 1734440044;
                            v4_int = 1633886309;
                            v3_int = 0x6f6e206e; // "n on"
                            v2_int = 0x656d2074; // "t me"
                            *(int*)&v1_short = 1948284005; // "cv po"
                            *(int*)&v9_short = 0x74206568; // " he t"
                            __builtin_strcpy(&v7_char, "arget");
                            logDebugMessage(3, &logBuffer, 0);
                        }
                    }
                    int checkTemp = checkCount;
                    --checkCount;
                    flag_lt_3 = checkTemp < SOCK_STREAM;
                    flag_eq_3 = checkTemp == SOCK_STREAM;
                    flag_sign = (((checkTemp - SOCK_STREAM) ^ checkTemp) & (checkTemp ^ SOCK_STREAM)) < 0;
                    flag_ge_3 = checkTemp >= SOCK_STREAM;
                    if(checkCount != 0) {
                        continue;
                    }
                    break;
                }
                multiplyFloat64((uint64_t)intermediateValue, (uint64_t)(intermediateValue >>> 0x40X), 0x3feccccccccccccdL);
                compareFloat64(floatComparison, (uint64_t)intermediateValue);
                moveFloatFlags((uint8_t)flag_sign | ((uint8_t)flag_ge_3 << SOCK_STREAM) | ((uint8_t)flag_eq_3 << 2) | ((uint8_t)0 << 3), comparisonResult);
                status = 0;
            voltageAdjusted:
                uint32_t retryCounter = 0;
                while(SOCK_STREAM) {
                    int isPowerGood = checkPowerGood();
                    ++retryCounter;
                    debugLevel = (retryCounter * 0xcccccccdL) >>> 32L;
                    if(isPowerGood != 0) {
                        int statusCheck = (int)(retryCounter - (debugLevel >>> 2) * 5);
                        if(statusCheck == 0 && globalDebugLevel > 3) {
                            logBuffer = 1836345698; // "stat"
                            v6_int = 1601071457; // "us"
                            v5_int = 0x5f746567; // "_get"
                            v4_int = 0x65776f70; // "powe"
                            __builtin_strcpy(&v3_int, "r_status failed!");
                            logDebugMessage(3, &logBuffer, statusCheck);
                        }
                        sleep(3);
                        if(retryCounter == 20) {
                            break;
                        }
                        continue;
                    }
                    return status;
                }
                return -1;
            }
            if(globalDebugLevel > 3) {
                __builtin_strcpy(&logBuffer, "read_feedback_voltage failed!");
                logDebugMessage(3, &logBuffer, 0);
            }
            status = 0;
            alertPowerFailure(9, "power voltage read failed, pls check!", dataSize);
            goto voltageAdjusted;
        }
        status = -1;
        goto voltageAdjusted;
    }
    if(globalDebugLevel > 3) {
        snprintf(&logBuffer, 0x800, "%s power iic error.\n", "power_init");
        logDebugMessage(3, &logBuffer, 0);
    }
    return status;
}
```