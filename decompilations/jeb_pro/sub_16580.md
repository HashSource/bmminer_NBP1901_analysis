```c
int readVoltageFrequencyConfig() {
    int functionResult;
    uint32_t levelId;
    char* formatString;
    uint32_t* minerTypeStrPointer;
    uint32_t hardwareVersion;
    int formattedStringBuffer;
    int tempVar;
    char readBufferFlag;
    int loopIndex = 0;
    do {
        int checkResult = checkHardwareStatus(loopIndex);
        if(checkResult != 0) {
            resetHardware(loopIndex);
            waitForHardwareReset(10);
        }
        ++loopIndex;
    }
    while(loopIndex != 4);
    memset(&voltageFrequencyTable, 0, 160);
    char* fileContent = readFileToString("/etc/levels.json", 0, &readBufferFlag);
    int* fileContentPtr = (int*)fileContent;
    if((fileContentPtr != 0 && fileContentPtr[0] == 0)) {
        uint32_t* firstLine = readLineFromFile((int*)fileContent, "\n");
        uint32_t* linePointer = firstLine;
        hardwareVersion = globalHardwareVersion;
        if(linePointer != 0 && linePointer[0] == 2) {
            if(hardwareVersion > 4) {
                minerTypeStrPointer = extractStringFromLine(linePointer);
                snprintf(&formattedStringBuffer, 0x800, "miner type in %s is %s", "/etc/levels.json", minerTypeStrPointer);
                logMessage(4, &formattedStringBuffer, 0);
            }
            uint32_t* minerType = extractStringFromLine(linePointer);
            char* foundString = strstr(&expectedMinerType, (char*)minerType);
            if(foundString != 0) {
                uint32_t* expectedType = extractStringFromLine(linePointer);
                int compareResult = strcasecmp(foundString, (char*)expectedType);
                if(compareResult == 0) {
                    uint32_t* levelsLine = readLineFromFile(fileContentPtr, "levels");
                    if((levelsLine == 0 || levelsLine[0] != SOCK_STREAM) && globalHardwareVersion <= 4) {
                        return -2;
                    }
                    else if(levelsLine == 0 || levelsLine[0] != SOCK_STREAM) {
                        formatString = "failed to get levels in %s\n";
                        snprintf(&formattedStringBuffer, 0x800, formatString, "/etc/levels.json");
                        logMessage(4, &formattedStringBuffer, 0);
                        return -2;
                    }
                    int numEntries = 0;
                    int levelReadCount = 0;
                    char* levelsDelimiter = "\n";
                    int levelsResult = 0;
                    int* globalHardwareVersionPtr = (int*)&globalHardwareVersion;
                    while(SOCK_STREAM) {
                        uint32_t* nextLevelLine = readNextLine(levelsLine);
                        int currentReadIndex = levelReadCount;
                        int isNewLine = (uint32_t)nextLevelLine > (uint32_t)levelReadCount;
                        int* nextLevelPtr = (int*)levelsLine;
                        if(numEntries > 19) {
                            isNewLine = 0;
                        }
                        ++levelReadCount;
                        if(isNewLine == 0) {
                            break;
                        }
                        int* levelString = fetchLevelString(nextLevelPtr, currentReadIndex);
                        int* levelStringPtr = levelString;
                        if(fileContentPtr[0] != 0) {
                            continue;
                        }
                        uint32_t* voltageLine = readLineFromFile(levelString, "\n");
                        if(voltageLine == 0 || voltageLine[0] != 3) {
                            continue;
                        }
                        int voltageFrequencyIndex = numEntries * 8 + &voltageFrequencyTableOffset;
                        int voltageValue = extractValueFromLine((int*)voltageLine);
                        char* levelsDelimiterPointer = levelsDelimiter;
                        *(int*)(voltageFrequencyIndex + 8) = voltageValue;
                        uint32_t* frequencyLine = readLineFromFile(levelStringPtr, levelsDelimiterPointer);
                        if(frequencyLine == 0 || frequencyLine[0] != 3) {
                            continue;
                        }
                        int frequencyValue = extractValueFromLine((int*)frequencyLine);
                        ++numEntries;
                        *(int*)(voltageFrequencyIndex + 4) = frequencyValue;
                    }
                    functionResult = levelsResult;
                    int* globalVersionPtr = globalHardwareVersionPtr;
                    int logIndex = 0;
                    do {
                        levelId = *(uint32_t*)(logIndex * 8 + (int)&voltageFrequencyTable);
                        int levelOffset = logIndex * 8;
                        ++logIndex;
                        int* voltageFrequencyPair = (int*)(levelOffset + &voltageFrequencyTable);
                        if(levelId != 0) {
                            int frequency = *(voltageFrequencyPair + SOCK_STREAM);
                            if(frequency != 0 && (uint32_t)*globalVersionPtr > 4) {
                                int frequencyLog = frequency;
                                minerTypeStrPointer = levelId;
                                snprintf(&formattedStringBuffer, 0x800, "level %d, voltage = %d frequency = %d", logIndex, minerTypeStrPointer, frequencyLog);
                                logMessage(4, &formattedStringBuffer, 0);
                            }
                        }
                    }
                    while(logIndex != 20);
                    if(fileContentPtr[SOCK_STREAM] != -1) {
                        hardwareVersion = fileContentPtr + SOCK_STREAM;
                        DMB();
                        levelId = *(int*)hardwareVersion - SOCK_STREAM;
                        *(uint32_t*)hardwareVersion = (uint32_t)(*(int*)hardwareVersion - SOCK_STREAM);
                        if(levelId == 0) {
                            freeMemory(fileContentPtr);
                            return functionResult;
                        }
                    }
                    return functionResult;
                }
            }
            if(globalHardwareVersion <= 3) {
                return -3;
            }
            formattedStringBuffer = 0x656e696d;
            int strPart1 = 2037653618;
            int strPart2 = 1847616880;
            int strPart3 = 1830843503;
            __builtin_strcpy(&tempVar, "atch");
            logMessage(3, &formattedStringBuffer, 0);
            return -3;
        }
        if(hardwareVersion <= 4) {
            return -2;
        }
        formatString = "failed to get miner type in %s\n";
        snprintf(&formattedStringBuffer, 0x800, formatString, "/etc/levels.json");
        logMessage(4, &formattedStringBuffer, 0);
        return -2;
    }
    else if(globalHardwareVersion <= 3) {
        functionResult = -1;
    }
    else {
        snprintf(&formattedStringBuffer, 0x800, "load vol-freq config file %s failed\n", "/etc/levels.json");
        logMessage(3, &formattedStringBuffer, 0);
        return -1;
    }
    return functionResult;
}
```